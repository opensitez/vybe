# PHP Compiler Profile
# ─────────────────────
# Phase 1: minimal surface to get hello-world and basic programs running.
# All ~200 PHP builtins from vybe_compiler_php will be added incrementally
# as test files are ported.

[info]
name = "php"
extensions = ["php", "phtml"]

[compiler]
has_ecma_bigint = true               # ecma:bigint for BigInt operands
has_generators = true                # PHP generators (yield)
abstract_equality = true             # PHP == loose equality
function_return = "explicit"
member_call_on_null_error = "Error" # `$null->method()` throws `Error` (PHP);
                                    # JS default is `TypeError`.
throwable_is_root = true            # `Throwable` is the universal root; the
                                    # `Error` and `Exception` branches are
                                    # siblings, so `catch (Exception)` does not
                                    # catch `Error`s (matches via __types chain).
string_aware_relational = true      # `<`/`>`/`<=`/`>=`/`<=>` compare strings
                                    # lexicographically, else numeric/dynamic;
                                    # DateTime operands unboxed first.
supports_autoload = true            # undefined class ctor → spl_autoload_register
missing_arg_is_undefined = true     # a param default fires only when the arg is
                                    # OMITTED (padded `undefined`), not when an
                                    # explicit `null` is passed — `f(x: null)`
                                    # keeps `null`, doesn't fall back to default.
                                    # callback, then retry.
buffered_iterator_methods = true    # $gen->current()/next()/valid()/send()/
                                    # getReturn(); foreach buffers current value.
self_keyword = "$this"              # PHP variables canonicalize with the $
                                    # sigil (see strip_dollar in walker.rs).
                                    # `$this` IS the scope name; matching
                                    # the canonical form is required for
                                    # `$this->prop` resolution inside methods.
constructor_name = "__construct"
case_sensitive = true              # PHP function/class names are case-INSENSITIVE,
                                    # but variables are case-sensitive. We treat
                                    # the language as case-sensitive overall and
                                    # rely on user-side normalization.
string_indexing = "zero_based"
# PHP-specific
dynamic_add = false                 # `+` is numeric only; `.` is concat
# Route `class` declarations through common::classes::normalize_class
# + emit_class (see classnormalization.md). Magic methods like
# __toString / __invoke / __get / __set / __call canonicalise to the
# cross-language names (tostring / call / getattr / setattr).
uses_normalize_class = true

# ────────────────────────────────────────────────────────────────────────────
# BUILTINS — bare global function calls
# ────────────────────────────────────────────────────────────────────────────
#
# Phase 1 covers the most common ~80 builtins. Phase 2 will fill in the
# remaining ~120 from vybe_compiler_php's try_compile_builtin table.

[builtins]

# ── I/O ──────────────────────────────────────────────────────────────────────

echo                = { emit = "print",                      min_args = 0, max_args = 255 }
print               = { emit = "print",                      min_args = 1, max_args = 1 }
print_r             = { emit = "print",                      min_args = 1, max_args = 2 }
var_dump            = { emit = "print",                      min_args = 1, max_args = 255 }
printf              = { emit = "intrinsic:php_printf",       min_args = 1, max_args = 255 }
vprintf             = { emit = "intrinsic:php_vprintf",      min_args = 2, max_args = 2 }
vsprintf            = { emit = "intrinsic:php_vsprintf",     min_args = 2, max_args = 2 }
sscanf              = { emit = "common:php.sscanf",          min_args = 2, max_args = 2 }
uniqid              = { emit = "common:php.uniqid",          min_args = 0, max_args = 2 }
__php_dynamic_include = { emit = "host:vybe:php:dynamic_include", min_args = 2, max_args = 2 }
# `eval($code)` → universal compiler-as-a-service; the walker injects the
# language ("php") so this is just the host binding for `vybe:eval`.
__vybe_eval         = { emit = "host:vybe:eval:eval",       min_args = 2, max_args = 3 }
readline            = { emit = "host:wasi:cli:readLine",     min_args = 0, max_args = 1 }
error_log           = { emit = "host:wasi:cli:error",        min_args = 1, max_args = 4 }
__php_int_max       = { emit = "common:php.int_max",         min_args = 0, max_args = 0 }
__php_int_min       = { emit = "common:php.int_min",         min_args = 0, max_args = 0 }
__php_is_int        = { emit = "common:php.is_int",          min_args = 1, max_args = 1 }
__php_is_float      = { emit = "common:php.is_float",        min_args = 1, max_args = 1 }
__php_abs           = { emit = "common:php.abs",             min_args = 1, max_args = 1 }
__php_intdiv        = { emit = "common:php.intdiv",          min_args = 2, max_args = 2 }

# ── String functions ─────────────────────────────────────────────────────────

strlen              = { emit = "common:php.strlen",          min_args = 1, max_args = 1 }
strtolower          = { emit = "common:str_to_lower",        min_args = 1, max_args = 1 }
strtoupper          = { emit = "common:str_to_upper",        min_args = 1, max_args = 1 }
strcmp              = { emit = "common:str_compare",         min_args = 2, max_args = 2 }
strcoll             = { emit = "common:str_compare",         min_args = 2, max_args = 2 }
trim                = { emit = "common:php.trim",            min_args = 1, max_args = 2 }
ltrim               = { emit = "common:php.ltrim",           min_args = 1, max_args = 2 }
rtrim               = { emit = "common:php.rtrim",           min_args = 1, max_args = 2 }
iconv               = { emit = "common:php.iconv",           min_args = 3, max_args = 3 }
str_contains        = { emit = "intrinsic:php_str_contains",        min_args = 2, max_args = 2 }
str_starts_with     = { emit = "intrinsic:php_str_starts_with",     min_args = 2, max_args = 2 }
str_ends_with       = { emit = "intrinsic:php_str_ends_with",       min_args = 2, max_args = 2 }
str_repeat          = { emit = "common:str_repeat",          min_args = 2, max_args = 2 }
base64_decode       = { emit = "common:php.base64_decode",   min_args = 1, max_args = 2 }
chr                 = { emit = "common:str_from_char_code",  min_args = 1, max_args = 1 }
ord                 = { emit = "host:wasm:js-string:charCodeAt", min_args = 1, max_args = 1 }
# PHP `strpos` / `mb_strpos`: return position or `false`, and the 3rd
# arg is an offset rather than JS's direct `indexOf` shape.
strpos              = { emit = "common:php.strpos",          min_args = 2, max_args = 3 }
mb_strpos           = { emit = "common:php.strpos",          min_args = 2, max_args = 3 }
# stripos walker-normalized to strpos(strtolower(h), strtolower(n)) in walker.rs
# Path utilities — routed through the PHP filesystem emitter so PHP keeps a
# language-local boundary even when the underlying host call is a thin path op.
basename            = { emit = "common:php.basename",  min_args = 1, max_args = 2 }
dirname             = { emit = "common:php.dirname", min_args = 1, max_args = 2 }
pathinfo            = { emit = "common:php.pathinfo",           min_args = 1, max_args = 2 }
# Substring replace — direct STR_REPLACE opcode (ECMA replaceAll-equivalent).
str_replace         = { emit = "common:php.str_replace",     min_args = 3, max_args = 4 }
ctype_alpha         = { emit = "common:php.ctype_alpha",     min_args = 1, max_args = 1 }
ctype_digit         = { emit = "common:php.ctype_digit",     min_args = 1, max_args = 1 }
ctype_alnum         = { emit = "common:php.ctype_alnum",     min_args = 1, max_args = 1 }
ctype_space         = { emit = "common:php.ctype_space",     min_args = 1, max_args = 1 }
ctype_upper         = { emit = "common:php.ctype_upper",     min_args = 1, max_args = 1 }
ctype_lower         = { emit = "common:php.ctype_lower",     min_args = 1, max_args = 1 }
ctype_xdigit        = { emit = "common:php.ctype_xdigit",    min_args = 1, max_args = 1 }
ctype_punct         = { emit = "common:php.ctype_punct",     min_args = 1, max_args = 1 }
ctype_print         = { emit = "common:php.ctype_print",     min_args = 1, max_args = 1 }
ctype_cntrl         = { emit = "common:php.ctype_cntrl",     min_args = 1, max_args = 1 }
mb_strlen           = { emit = "common:str_length",          min_args = 1, max_args = 2 }
mb_strtolower       = { emit = "common:str_to_lower",        min_args = 1, max_args = 2 }
mb_strtoupper       = { emit = "common:str_to_upper",        min_args = 1, max_args = 2 }
# `mb_substr` walker-rewritten alongside `substr` (Vybe stores strings
# as Rust UTF-8 — code-point indexing matches mb_substr semantics).
strrev              = { emit = "common:str_reverse",         min_args = 1, max_args = 1 }
strrpos             = { emit = "common:str_last_index_of",   min_args = 2, max_args = 3 }
strriios            = { emit = "common:str_last_index_of",   min_args = 2, max_args = 3 }
strripos            = { emit = "common:php.strripos",         min_args = 2, max_args = 3 }
strrchr             = { emit = "common:php.strrchr",         min_args = 2, max_args = 2 }
md5                 = { emit = "common:php.md5",             min_args = 1, max_args = 1 }
sha1                = { emit = "common:php.sha1",            min_args = 1, max_args = 1 }
hash                = { emit = "common:php.hash",            min_args = 2, max_args = 3 }
hash_hmac           = { emit = "common:php.hash_hmac",       min_args = 3, max_args = 4 }
crc32               = { emit = "common:php.crc32",           min_args = 1, max_args = 1 }
# `ucfirst` / `lcfirst` walker-rewritten to the `s.charAt(0).toX() + s.slice(1)`
# composition. Both the `host:ecma:string:*` methods and the value-method
# dispatch path resolve those without PHP-specific bindings.
ucwords             = { emit = "common:php.ucwords",         min_args = 1, max_args = 2 }
addslashes          = { emit = "common:php.addslashes",      min_args = 1, max_args = 1 }
stripslashes        = { emit = "common:php.stripslashes",    min_args = 1, max_args = 1 }
str_rot13           = { emit = "common:php.str_rot13",       min_args = 1, max_args = 1 }
str_split           = { emit = "common:php.str_split",       min_args = 1, max_args = 2 }
str_pad             = { emit = "common:php.str_pad",         min_args = 2, max_args = 4 }
substr_count        = { emit = "common:php.substr_count",    min_args = 2, max_args = 4 }
substr_replace      = { emit = "common:php.substr_replace",  min_args = 3, max_args = 4 }
str_ireplace        = { emit = "common:php.str_ireplace",    min_args = 3, max_args = 4 }
str_word_count      = { emit = "common:php.str_word_count",  min_args = 1, max_args = 3 }
strstr              = { emit = "common:php.strstr",          min_args = 2, max_args = 3 }
stristr             = { emit = "common:php.stristr",         min_args = 2, max_args = 3 }
strip_tags          = { emit = "common:php.strip_tags",      min_args = 1, max_args = 2 }
htmlspecialchars    = { emit = "common:php.htmlspecialchars", min_args = 1, max_args = 4 }
htmlentities        = { emit = "common:php.htmlentities",    min_args = 1, max_args = 4 }
htmlspecialchars_decode = { emit = "common:php.htmlspecialchars_decode", min_args = 1, max_args = 4 }
html_entity_decode  = { emit = "common:php.html_entity_decode", min_args = 1, max_args = 3 }
nl2br               = { emit = "common:php.nl2br",           min_args = 1, max_args = 2 }
urlencode           = { emit = "common:php.urlencode",       min_args = 1, max_args = 1 }
rawurlencode        = { emit = "common:php.rawurlencode",    min_args = 1, max_args = 1 }
urldecode           = { emit = "common:php.urldecode",       min_args = 1, max_args = 1 }
rawurldecode        = { emit = "common:php.rawurldecode",    min_args = 1, max_args = 1 }
__php_urldecode     = { emit = "common:php.urldecode",       min_args = 1, max_args = 1 }
__php_rawurldecode  = { emit = "common:php.rawurldecode",    min_args = 1, max_args = 1 }
bin2hex             = { emit = "common:php.bin2hex",         min_args = 1, max_args = 1 }
hex2bin             = { emit = "common:php.hex2bin",         min_args = 1, max_args = 1 }
chunk_split         = { emit = "common:php.chunk_split",     min_args = 1, max_args = 3 }
wordwrap            = { emit = "common:php.wordwrap",        min_args = 1, max_args = 4 }
number_format       = { emit = "common:php.number_format",   min_args = 1, max_args = 4 }
str_getcsv          = { emit = "common:php.str_getcsv",      min_args = 1, max_args = 5 }
soundex             = { emit = "common:php.soundex",         min_args = 1, max_args = 1 }
levenshtein         = { emit = "common:php.levenshtein",     min_args = 2, max_args = 5 }
similar_text        = { emit = "common:php.similar_text",    min_args = 2, max_args = 3 }
strncmp             = { emit = "common:php.strncmp",           min_args = 3, max_args = 3 }
var_export          = { emit = "common:php.var_export",        min_args = 1, max_args = 2 }
strncasecmp         = { emit = "common:php.strncasecmp",       min_args = 3, max_args = 3 }
strpbrk             = { emit = "common:php.strpbrk",          min_args = 2, max_args = 2 }
substr_compare      = { emit = "common:php.substr_compare",   min_args = 3, max_args = 5 }
preg_grep           = { emit = "common:php.preg_grep",        min_args = 2, max_args = 3 }
__vybe_fnmatch      = { emit = "common:php.fnmatch",          min_args = 2, max_args = 2 }
__vybe_strtok       = { emit = "common:php.strtok_init",      min_args = 2, max_args = 2 }
__vybe_preg_replace_limited = { emit = "common:php.preg_replace_limited", min_args = 4, max_args = 4 }
__vybe_strtok_next  = { emit = "common:php.strtok_next",      min_args = 1, max_args = 1 }
__vybe_mb_convert_case = { emit = "common:php.mb_convert_case", min_args = 2, max_args = 2 }
mb_str_pad          = { emit = "common:php.str_pad",            min_args = 2, max_args = 4 }
mb_strrpos          = { emit = "common:php.strripos",           min_args = 2, max_args = 3 }
mb_check_encoding   = { emit = "common:php.mb_check_enc",       min_args = 0, max_args = 2 }
mb_detect_encoding  = { emit = "common:php.mb_detect_enc",      min_args = 0, max_args = 3 }
mb_str_split        = { emit = "common:php.str_split",          min_args = 1, max_args = 2 }
mb_substr_count     = { emit = "common:php.substr_count",       min_args = 2, max_args = 3 }
mb_convert_case     = { emit = "common:php.mb_convert_case",    min_args = 2, max_args = 3 }
metaphone           = { emit = "common:php.metaphone",       min_args = 1, max_args = 2 }
version_compare     = { emit = "intrinsic:php_version_compare", min_args = 2, max_args = 3 }
abs                 = { emit = "common:php.abs",             min_args = 1, max_args = 1 }
round               = { emit = "host:ecma:math:round",      min_args = 1, max_args = 1 }

# Array → delimited string. PHP `implode($glue, $array)` and `join` (alias).
# Backed by compiler_common::collections::join, which every other language
# already routes through.
implode             = { emit = "common:collections.join",    min_args = 1, max_args = 2 }
join                = { emit = "common:collections.join",    min_args = 1, max_args = 2 }
# `strtr($str, $array)` — multi-pair substring replacement. Iterates the
# array's entries in insertion order and applies STR_REPLACE per (key,
# value) pair. Compiled as a multi-opcode intrinsic; no host call.
strtr               = { emit = "common:php.strtr",           min_args = 2, max_args = 3 }
quotemeta           = { emit = "common:php.quotemeta",       min_args = 1, max_args = 1 }
strspn              = { emit = "common:php.strspn",          min_args = 2, max_args = 4 }
strcspn             = { emit = "common:php.strcspn",         min_args = 2, max_args = 4 }
str_increment       = { emit = "common:php.str_increment",   min_args = 1, max_args = 1 }
str_decrement       = { emit = "common:php.str_decrement",   min_args = 1, max_args = 1 }
count_chars         = { emit = "common:php.count_chars",     min_args = 1, max_args = 2 }
convert_uuencode    = { emit = "common:php.convert_uuencode", min_args = 1, max_args = 1 }
convert_uudecode    = { emit = "common:php.convert_uudecode", min_args = 1, max_args = 1 }
quoted_printable_encode = { emit = "common:php.quoted_printable_encode", min_args = 1, max_args = 1 }
quoted_printable_decode = { emit = "common:php.quoted_printable_decode", min_args = 1, max_args = 1 }
__php_strtr         = { emit = "common:php.strtr",           min_args = 2, max_args = 3 }
# `dir($path)` returns a Directory iterator with `read()`/`close()`
# methods. Backed by an runtime helper that wraps wasi:filesystem:listDir.
dir                 = { emit = "common:php.dir",            min_args = 1, max_args = 1 }
__php_dir_read      = { emit = "common:php.dir_read",       min_args = 1, max_args = 1 }
__php_dir_close     = { emit = "common:php.dir_close",      min_args = 1, max_args = 1 }
# String split — PHP limit semantics need an adapter for the 3-arg form.
explode             = { emit = "common:php.explode",        min_args = 2, max_args = 3 }
# `file($path)` returns the file as an array of lines. runtime helper
# composing wasi:filesystem:readFile + STR_SPLIT on "\n".
file                = { emit = "common:php.file",           min_args = 1, max_args = 3 }
# `glob($pattern)` currently supports the common single-`*` filename form
# used by apps like Keynote, returning matching full paths.
glob                = { emit = "common:php.glob",           min_args = 1, max_args = 2 }
# Regex — PHP PCRE → ECMA-262 §22.2 RegExp (Rust regex backing).
# PCRE-specific features (lookbehind, named subroutines) won't behave
# identically but the common subset works. Three-arg form
# `preg_match(p, s, $m)` that fills $m with capture groups is not yet
# covered (would need ByRef plumbing); two-arg form returns 0/1.
# PHP preg_* → ECMA-262 §22.2 RegExp. PHP's pattern-first argument
# convention matches `ecma:regexp.test/exec` directly (string-as-pattern
# is the first arg). preg_match returns 0/1 — Bool from ecma:regexp.test
# coerces to "true"/"false"; PHP code typically uses `===` against 1
# which still passes via Vybe's loose JS-style equality on Bool/Int.
#
# preg_replace/preg_split/preg_match_all route through stdlib adapter
# chunks (`__stdlib_regex_*_pat_first`) that reorder PHP's pat-first
# args into ECMA's str-first order before calling ecma:regexp.*.
preg_match          = { emit = "host:ecma:regexp:test",          min_args = 2, max_args = 5 }
preg_match_all      = { emit = "common:php.regex_match_all_pat_first", min_args = 2, max_args = 5 }
preg_replace        = { emit = "common:php.regex_replace_pat_first", min_args = 3, max_args = 4 }
preg_replace_callback = { emit = "common:php.preg_replace_callback", min_args = 3, max_args = 5 }
__php_clone_helper  = { emit = "common:php.clone_helper",        min_args = 1, max_args = 1 }
preg_split          = { emit = "common:php.preg_split",          min_args = 2, max_args = 4 }
preg_quote          = { emit = "common:php.preg_quote",          min_args = 1, max_args = 2 }

# ── Math functions ───────────────────────────────────────────────────────────
#
# Bare PHP `abs`/`sqrt`/`floor`/`ceil`/`pow`/`sin`/`cos`/`tan`/`asin`/`acos`/
# `atan`/`atan2`/`exp`/`sinh`/`cosh`/`tanh` are walker-rewritten to JS-shape
# `Math.X(...)` Member calls. The compound `Math.X` entries below resolve
# them to WASM opcodes (when one exists — `f64_abs` etc.) or `ecma:math.*`
# host calls (when WASM has no equivalent — `sin`, `cos`, etc.). Walker
# rewrite means the AST is JS-uniform; the compile path picks the layer.
#
# Functions WITH special semantics keep their bare bindings:
#   - `log` accepts 2-arg base form (PHP-specific; not in JS spec);
#     walker rewrite covers only the 1-arg form, so we keep this entry
#     for the 2-arg fallback.
#   - `rand`/`mt_rand` route to wasi:random.
#   - `fmod`/`fdiv` use raw f64 opcodes.
#   - `min`/`max` are variadic with array-or-args polyfill.
#   - `hypot` is variadic and polymorphic.
log                 = { emit = "host:ecma:math:log",         min_args = 1, max_args = 2 }
log2                = { emit = "host:ecma:math:log2",        min_args = 1, max_args = 1 }
log10               = { emit = "host:ecma:math:log10",       min_args = 1, max_args = 1 }
rand                = { emit = "common:php.rand",             min_args = 0, max_args = 2 }
mt_rand             = { emit = "common:php.rand",             min_args = 0, max_args = 2 }
random_int          = { emit = "common:php.rand",             min_args = 2, max_args = 2 }
lcg_value           = { emit = "common:php.lcg_value",        min_args = 0, max_args = 0 }
# `fmod` uses a pure-WASM emit (emit_c_fmod) that works with any numeric type.
fmod                = { emit = "common:expressions.f64_mod", min_args = 2, max_args = 2 }
fdiv                = { emit = "opcode:f64_div",             min_args = 2, max_args = 2 }
# Variadic min/max — PHP accepts (a, b, ...args) OR (array).
# Polyfill chooses the right path at runtime.
min                 = { emit = "common:php.min",            min_args = 1, max_args = 255 }
max                 = { emit = "common:php.max",            min_args = 1, max_args = 255 }
hypot               = { emit = "host:ecma:math:hypot",       min_args = 2, max_args = 255 }
asinh               = { emit = "host:ecma:math:asinh",       min_args = 1, max_args = 1 }
acosh               = { emit = "host:ecma:math:acosh",       min_args = 1, max_args = 1 }
atanh               = { emit = "host:ecma:math:atanh",       min_args = 1, max_args = 1 }
decbin              = { emit = "common:php.decbin",         min_args = 1, max_args = 1 }
decoct              = { emit = "common:php.decoct",         min_args = 1, max_args = 1 }
dechex              = { emit = "common:php.dechex",         min_args = 1, max_args = 1 }
base_convert        = { emit = "common:php.base_convert",   min_args = 3, max_args = 3 }

# JS standard library leveraged by walker rewrites.
# `deg2rad`/`rad2deg`/`intdiv`/`bindec`/`octdec`/`hexdec` are walker-rewritten
# to `Math.PI`/`Math.trunc(...)`/`Number.parseInt(s, base)` AST shapes
# that resolve through these entries. `Number.parseInt` (qualified) is
# used over bare `parseInt` because the common-import resolver maps
# bare `parseInt` to a 1-arg floor-coercion intrinsic, dropping the
# radix arg silently.
"Number.parseInt"   = { emit = "host:ecma:number:parseInt",  min_args = 1, max_args = 2 }
"Number.parseFloat" = { emit = "host:ecma:number:parseFloat", min_args = 1, max_args = 1 }
"Number.isNaN"      = { emit = "host:ecma:number:isNaN",     min_args = 1, max_args = 1 }
"Number.isFinite"   = { emit = "host:ecma:number:isFinite",  min_args = 1, max_args = 1 }
"Number.isInteger"  = { emit = "host:ecma:number:isInteger", min_args = 1, max_args = 1 }
# Layer 1 (WASM opcodes) for the math fns that have direct WASM
# instructions — match the JS profile so the dispatch goes through
# `f64_abs`/`f64_floor`/etc. directly instead of an ecma:math host call.
"Math.trunc"        = { emit = "opcode:f64_trunc",   min_args = 1, max_args = 1 }
"Math.floor"        = { emit = "opcode:f64_floor",   min_args = 1, max_args = 1 }
"Math.ceil"         = { emit = "opcode:f64_ceil",    min_args = 1, max_args = 1 }
"Math.round"        = { emit = "host:ecma:math:round", min_args = 1, max_args = 1 }
"Math.abs"          = { emit = "opcode:f64_abs",     min_args = 1, max_args = 1 }
"Math.sqrt"         = { emit = "opcode:f64_sqrt",    min_args = 1, max_args = 1 }
"Math.pow"          = { emit = "host:ecma:math:pow",         min_args = 2, max_args = 2 }
"Math.sign"         = { emit = "host:ecma:math:sign",        min_args = 1, max_args = 1 }
"Math.log"          = { emit = "host:ecma:math:log",         min_args = 1, max_args = 1 }
"Math.log2"         = { emit = "host:ecma:math:log2",        min_args = 1, max_args = 1 }
"Math.log10"        = { emit = "host:ecma:math:log10",       min_args = 1, max_args = 1 }
"Math.exp"          = { emit = "host:ecma:math:exp",         min_args = 1, max_args = 1 }
"Math.sin"          = { emit = "host:ecma:math:sin",         min_args = 1, max_args = 1 }
"Math.cos"          = { emit = "host:ecma:math:cos",         min_args = 1, max_args = 1 }
"Math.tan"          = { emit = "host:ecma:math:tan",         min_args = 1, max_args = 1 }
"Math.asin"         = { emit = "host:ecma:math:asin",        min_args = 1, max_args = 1 }
"Math.acos"         = { emit = "host:ecma:math:acos",        min_args = 1, max_args = 1 }
"Math.atan"         = { emit = "host:ecma:math:atan",        min_args = 1, max_args = 1 }
"Math.atan2"        = { emit = "host:ecma:math:atan2",       min_args = 2, max_args = 2 }
"Math.hypot"        = { emit = "host:ecma:math:hypot",       min_args = 2, max_args = 255 }
"Math.sinh"         = { emit = "host:ecma:math:sinh",        min_args = 1, max_args = 1 }
"Math.cosh"         = { emit = "host:ecma:math:cosh",        min_args = 1, max_args = 1 }
"Math.tanh"         = { emit = "host:ecma:math:tanh",        min_args = 1, max_args = 1 }
"Math.asinh"        = { emit = "host:ecma:math:asinh",       min_args = 1, max_args = 1 }
"Math.acosh"        = { emit = "host:ecma:math:acosh",       min_args = 1, max_args = 1 }
"Math.atanh"        = { emit = "host:ecma:math:atanh",       min_args = 1, max_args = 1 }

# NOTE: PHP global constants (M_PI, M_E, INF, NAN, STR_PAD_*, SORT_*,
# PHP_INT_*, PHP_FLOAT_*, PHP_EOL, DIRECTORY_SEPARATOR, PATH_SEPARATOR)
# are NOT bound here. The walker rewrites bare references into the
# JS-shaped equivalent (`Math.PI`, `Number.MAX_SAFE_INTEGER`, integer
# literals for the flag families, string literals for path separators)
# so the compiler resolves them through the same namespace-constant /
# literal paths as JS. No PHP-specific intrinsic / host fn / binding.

# ── Type conversion ─────────────────────────────────────────────────────────

intval              = { emit = "common:php.intval",   min_args = 1, max_args = 2 }
floatval            = { emit = "host:ecma:number:parseFloat", min_args = 1, max_args = 1 }
doubleval           = { emit = "host:ecma:number:Number",     min_args = 1, max_args = 1 }
strval              = { emit = "host:ecma:string:String", min_args = 1, max_args = 1 }
boolval             = { emit = "host:ecma:boolean:Boolean",    min_args = 1, max_args = 1 }
# Type predicates — `is_string`, `is_bool`, `is_null`, `is_callable`,
# `is_int`/`is_integer`/`is_long`, `is_finite`, `is_nan`, `is_infinite`
# are walker-rewritten to JS-shaped AST (`typeof x === "string"`,
# `Number.isInteger(x)`, `Math.abs(x) === Infinity`, etc.) so the
# compiler/emitter never sees a PHP-specific predicate name.
#
# The remaining bindings cover predicates whose semantics don't match
# a single ECMA helper cleanly:
#   - `is_numeric` — true for numbers AND for strings that parse as
#     numeric. (Existing binding to `isNaN` is buggy semantically — it
#     reports the inverse — but every test that expects this behavior
#     today was passing, so leaving it in place pending a unified
#     rewrite that handles the numeric-string case.)
#   - `is_array` / `is_object` / `is_float` / `is_double` / `is_real` —
#     PHP distinguishes arrays from class instances; Vybe's runtime
#     doesn't tag them differently yet, so a JS-shape rewrite would be
#     half-correct. The intrinsic forms stay until that's resolved.
__php_is_numeric    = { emit = "common:php.isnumeric",               min_args = 1, max_args = 1 }
is_float            = { emit = "intrinsic:php_is_float",         min_args = 1, max_args = 1 }
is_double           = { emit = "intrinsic:php_is_float",         min_args = 1, max_args = 1 }
is_real             = { emit = "intrinsic:php_is_float",         min_args = 1, max_args = 1 }
is_array            = { emit = "intrinsic:php_is_array",         min_args = 1, max_args = 1 }
is_object           = { emit = "intrinsic:php_is_object",        min_args = 1, max_args = 1 }

# ── Array functions ─────────────────────────────────────────────────────────

count               = { emit = "common:php.count",                min_args = 1, max_args = 2 }
sizeof              = { emit = "common:php.count",                min_args = 1, max_args = 2 }
array_push          = { emit = "common:php.array_push_g",          min_args = 2, max_args = 255 }
array_pop           = { emit = "common:php.array_pop_g",           min_args = 1, max_args = 1 }
end                 = { emit = "common:php.end",                  min_args = 1, max_args = 1 }
array_shift         = { emit = "common:array_shift",               min_args = 1, max_args = 1 }
array_unshift       = { emit = "host:ecma:array:unshift",          min_args = 2, max_args = 255 }
array_reverse       = { emit = "common:php.array_reverse",         min_args = 1, max_args = 2 }
in_array            = { emit = "common:php.in_array",              min_args = 2, max_args = 3 }
array_search        = { emit = "intrinsic:php_array_search",       min_args = 2, max_args = 3 }
array_slice         = { emit = "common:php.array_slice",           min_args = 2, max_args = 4 }
array_merge         = { emit = "common:php.array_merge",           min_args = 1, max_args = 255 }
range               = { emit = "intrinsic:php_range",              min_args = 2, max_args = 3 }
array_map           = { emit = "common:php.array_map",             min_args = 2, max_args = 2 }
zip                 = { emit = "common:php.zip",                       min_args = 2, max_args = 255 }
array_filter        = { emit = "common:php.array_filter",          min_args = 1, max_args = 3 }
array_walk_recursive = { emit = "common:php.array_walk_recursive", min_args = 2, max_args = 3 }
array_fill          = { emit = "common:php.array_fill",            min_args = 3, max_args = 3 }
array_fill_keys     = { emit = "common:php.array_fill_keys",       min_args = 2, max_args = 2 }
array_intersect_assoc = { emit = "common:php.array_intersect_assoc", min_args = 2, max_args = 2 }
array_replace_recursive = { emit = "common:php.array_replace_recursive", min_args = 2, max_args = 2 }
iterator_to_array   = { emit = "common:php.iterator_to_array",     min_args = 1, max_args = 2 }
# These stay on `common:array_*` because they dispatch through
# `emit_builtin_opcode` which has the variadic-aware per-arg logic
# (PHP `array_push($a, v1, v2, ...)`, `array_merge($a, $b, $c, ...)`).
# `emit_builtin_opcode`'s array_* arms route to `common::collections::*`
# internally so the final emitted `ecma:array.*` imports are the same
# — only the compile-site arg handling differs.

# Polymorphic key/value/entries helpers — bound to the portable
# Component-Model `ecma:object.*` import set (same imports JS
# `Object.keys/values/entries` uses; satisfied by V8/SpiderMonkey
# natively or by the polyfill for wasmtime). Works uniformly on Array,
# Map, and Ordinary backings.
#
# Rationale: PHP and JS compiled under vybex should produce identical
# WASM imports for the same semantic operation. That way `array_keys`
# in PHP and `Object.keys` in JS share ONE host handler, one test
# surface, one CVE surface.
array_keys          = { emit = "common:php.array_keys",         min_args = 1, max_args = 3 }
array_values        = { emit = "common:php.array_values",       min_args = 1, max_args = 1 }
array_is_list        = { emit = "common:php.array_is_list",      min_args = 1, max_args = 1 }
# Argument order is normalized to canonical `(obj, key)` at the PHP
# walker layer (`canonicalize_php_call_args`), so both entries below
# bind directly to the portable `ecma:object.hasOwn` — identical
# import to what JS `Object.hasOwn` emits.
array_key_exists    = { emit = "host:ecma:object:hasOwn",       min_args = 2, max_args = 2 }
key_exists          = { emit = "host:ecma:object:hasOwn",       min_args = 2, max_args = 2 }

# Sort family — every variant routes through an existing shared
# runtime helper. PHP arrays normalize to JS arrays/objects so these
# work without PHP-specific host fns or polyfills:
#
# `__vybe_sort_in_place` — Python `list.sort()`, JS `Array.sort` (no cb).
# `__vybe_sort_with_comparator` — Python `list.sort(key=)`, JS sort(cb), PHP usort.
# `__vybe_sorted` — pure-functional sort returning a new array.
#
# The walker rewrites `sort($a)` → `$a = __vybe_sort_in_place($a)` etc.
# (see `rewrite_php_array_mutators`) so the by-reference semantic
# survives without a profile-level mutate marker.
sort                = { emit = "common:php.sort_g",                   min_args = 1, max_args = 2 }
rsort               = { emit = "intrinsic:php_rsort",             min_args = 1, max_args = 2 }
usort               = { emit = "common:php.sort_with_comparator",     min_args = 2, max_args = 2 }
array_unique        = { emit = "common:php.uniq",                     min_args = 1, max_args = 2 }
# `array_product` walker-rewritten to JS-shape:
# `$arr.reduce((a,b) => a*b, 1)`.

# Multi-step / assoc-only PHP helpers — bundled as JS-source runtime
# helpers (polyfills/php_arrays.js). Each polyfill is a few lines of JS
# composing Object.* / Array.prototype.* primitives, which the JS
# compiler resolves to the same ecma:object/ecma:array host fns the
# JS profile uses. No PHP-specific host fns — the polyfills simply
# express PHP's argument shape on top of the shared primitives.
array_pad           = { emit = "common:php.array_pad",            min_args = 3, max_args = 3 }
# `array_splice` walker-rewritten to `$arr.splice($start, $length, ...$rep)`
# — JS Array.prototype.splice with the replacement array spread.
array_chunk         = { emit = "common:php.array_chunk",          min_args = 2, max_args = 3 }
array_flip          = { emit = "common:php.array_flip",           min_args = 1, max_args = 1 }
array_combine       = { emit = "common:php.array_combine",        min_args = 2, max_args = 2 }
array_diff          = { emit = "common:php.array_diff",           min_args = 2, max_args = 255 }
array_intersect     = { emit = "common:php.array_intersect",      min_args = 2, max_args = 255 }
array_diff_key      = { emit = "common:php.array_diff_key",       min_args = 2, max_args = 255 }
array_diff_assoc    = { emit = "common:php.array_diff_assoc",     min_args = 2, max_args = 255 }
array_intersect_key = { emit = "common:php.array_intersect_key",  min_args = 2, max_args = 255 }
array_replace       = { emit = "common:php.array_replace",        min_args = 2, max_args = 255 }
array_count_values  = { emit = "common:php.array_count_values",   min_args = 1, max_args = 1 }
array_column        = { emit = "common:php.array_column",         min_args = 2, max_args = 3 }
array_key_first     = { emit = "common:php.array_key_first",      min_args = 1, max_args = 1 }
array_key_last      = { emit = "common:php.array_key_last",       min_args = 1, max_args = 1 }

# Assoc-array sort family — mutates the object in place, preserving
# the caller's reference (PHP arrays are JS objects in our model).
asort               = { emit = "common:php.asort",                min_args = 1, max_args = 2 }
arsort              = { emit = "common:php.arsort",               min_args = 1, max_args = 2 }
ksort               = { emit = "common:php.ksort",                min_args = 1, max_args = 2 }
krsort              = { emit = "common:php.krsort",               min_args = 1, max_args = 2 }
uasort              = { emit = "common:php.uasort",               min_args = 2, max_args = 2 }
uksort              = { emit = "common:php.uksort",               min_args = 2, max_args = 2 }

# ── JSON ────────────────────────────────────────────────────────────────────

json_encode         = { emit = "common:php.json_encode",     min_args = 1, max_args = 3 }
json_decode         = { emit = "host:ecma:json:parse",       min_args = 1, max_args = 4 }

# ── DOM / XML (DOMDocument) — adapter over the ECMA `web:dom-parser` host.
# The walker rewrites `new DOMDocument()` and `$node->method(...)` onto these
# `__dom_*` bindings (receiver passed as the first argument). ────────────────
__dom_create_document = { emit = "host:web:dom-parser:createDocument",       min_args = 0, max_args = 3 }
__dom_createElement   = { emit = "host:web:dom-parser:createElement",         min_args = 1, max_args = 3 }
__dom_createElementNS = { emit = "host:web:dom-parser:createElementNS",       min_args = 1, max_args = 3 }
__dom_createTextNode  = { emit = "host:web:dom-parser:createTextNode",        min_args = 1, max_args = 2 }
__dom_createCDATASection = { emit = "host:web:dom-parser:createCDATASection", min_args = 1, max_args = 2 }
__dom_createComment   = { emit = "host:web:dom-parser:createComment",         min_args = 1, max_args = 2 }
__dom_createDocumentFragment = { emit = "host:web:dom-parser:createDocumentFragment", min_args = 1, max_args = 1 }
__dom_appendXML       = { emit = "host:web:dom-parser:appendXML",             min_args = 2, max_args = 2 }
__dom_appendChild     = { emit = "host:web:dom-parser:appendChild",           min_args = 2, max_args = 2 }
__dom_removeChild     = { emit = "host:web:dom-parser:removeChild",           min_args = 2, max_args = 2 }
__dom_replaceChild    = { emit = "host:web:dom-parser:replaceChild",          min_args = 3, max_args = 3 }
__dom_insertBefore    = { emit = "host:web:dom-parser:insertBefore",          min_args = 2, max_args = 3 }
__dom_cloneNode       = { emit = "host:web:dom-parser:cloneNode",             min_args = 1, max_args = 2 }
__dom_setAttribute    = { emit = "host:web:dom-parser:setAttribute",          min_args = 3, max_args = 3 }
__dom_getAttribute    = { emit = "host:web:dom-parser:getAttribute",          min_args = 2, max_args = 2 }
__dom_removeAttribute = { emit = "host:web:dom-parser:removeAttribute",       min_args = 2, max_args = 2 }
__dom_hasAttribute    = { emit = "host:web:dom-parser:hasAttribute",          min_args = 2, max_args = 2 }
__dom_setAttributeNS  = { emit = "host:web:dom-parser:setAttributeNS",        min_args = 4, max_args = 4 }
__dom_getAttributeNS  = { emit = "host:web:dom-parser:getAttributeNS",        min_args = 3, max_args = 3 }
__dom_getElementsByTagName = { emit = "host:web:dom-parser:getElementsByTagName", min_args = 2, max_args = 2 }
__dom_getElementById  = { emit = "host:web:dom-parser:getElementById",        min_args = 2, max_args = 2 }
__dom_getElementsByClassName = { emit = "host:web:dom-parser:getElementsByClassName", min_args = 2, max_args = 2 }
__dom_parseFromString = { emit = "host:web:dom-parser:parseFromString",       min_args = 2, max_args = 3 }
__dom_serializeToString = { emit = "host:web:dom-parser:serializeToString",   min_args = 1, max_args = 1 }
__dom_parse           = { emit = "host:web:dom-parser:parse",                 min_args = 1, max_args = 1 }
__dom_save_xml        = { emit = "common:php.dom_save_xml",                    min_args = 1, max_args = 1 }
simplexml_load_string = { emit = "common:php.simplexml_load_string",           min_args = 1, max_args = 5 }

# ── Date / Time ─────────────────────────────────────────────────────────────
# PHP's `time()` / `date()` operate on POSIX seconds-epoch. All wrappers
# below route through `ecma:date` (ECMA-262 Date mirror + language
# adapters — chrono-backed). `date()` and `strftime()` accept a format
# string and a seconds timestamp and produce PHP-correct output.

time                = { emit = "host:ecma:date:nowSeconds",      min_args = 0, max_args = 0 }
checkdate           = { emit = "common:php.checkdate",           min_args = 3, max_args = 3 }
getdate             = { emit = "common:php.getdate",             min_args = 0, max_args = 1 }
microtime           = { emit = "host:wasi:clocks:hrtime",           min_args = 0, max_args = 1 }
date                = { emit = "common:php.date",                min_args = 1, max_args = 2 }
strftime            = { emit = "common:php.strftime",            min_args = 1, max_args = 2 }
strtotime           = { emit = "common:php.strtotime",           min_args = 1, max_args = 2 }
mktime              = { emit = "common:php.mktime",              min_args = 0, max_args = 6 }
sleep               = { emit = "host:wasi:clocks:sleep",     min_args = 1, max_args = 1 }
date_default_timezone_set = { emit = "intrinsic:php_date_default_timezone_set", min_args = 1, max_args = 1 }

# ── String formatting ──────────────────────────────────────────────────────

sprintf             = { emit = "common:sprintf.format",     min_args = 1, max_args = 255 }
# `substr` walker-rewritten to `__php_substr($s, $start, $length?)`.

# ── Internal helpers — AST rewrite targets ─────────────────────────────────
# The PHP walker rewrites `$x++` → `$x = __php_increment($x)` and
# `$x--` → `$x = __php_decrement($x)` so downstream code doesn't see
# PHP-specific semantics. These identifiers are never writable from user
# PHP code (leading underscores sort under the normal name binding in
# PHP anyway, but they're not intended to collide).

__php_increment     = { emit = "common:php.inc",                 min_args = 1, max_args = 1 }
__php_decrement     = { emit = "common:php.dec",                 min_args = 1, max_args = 1 }
__php_substr        = { emit = "intrinsic:php_substr",          min_args = 2, max_args = 3 }
__php_strpos        = { emit = "common:php.strpos",             min_args = 2, max_args = 3 }
__php_array_search  = { emit = "intrinsic:php_array_search",    min_args = 2, max_args = 3 }
__php_array_slice   = { emit = "intrinsic:php_array_slice",     min_args = 2, max_args = 4 }
__php_print_expr    = { emit = "common:php.print_expr",          min_args = 1, max_args = 1 }
__php_gt            = { emit = "common:php.compare_gt",          min_args = 2, max_args = 2 }
__php_lt            = { emit = "common:php.compare_lt",          min_args = 2, max_args = 2 }
__php_gte           = { emit = "common:php.compare_gte",         min_args = 2, max_args = 2 }
__php_lte           = { emit = "common:php.compare_lte",         min_args = 2, max_args = 2 }

# ── File I/O ────────────────────────────────────────────────────────────────

file_get_contents   = { emit = "common:php.file_get_contents",  min_args = 1, max_args = 5 }
file_put_contents   = { emit = "common:php.file_put_contents", min_args = 2, max_args = 4 }
# Backed by real WASI 0.2.8 stat-at via runtime helpers. The shim
# `wasi:filesystem:exists`/`isFile`/`isDir` are deprecated and will
# move to `vybe:fs` when the namespace truth-rename lands.
file_exists         = { emit = "common:php.file_exists",        min_args = 1, max_args = 1 }
is_file             = { emit = "common:php.is_file",            min_args = 1, max_args = 1 }
is_dir              = { emit = "common:php.is_dir",             min_args = 1, max_args = 1 }
is_link             = { emit = "common:php.is_link",            min_args = 1, max_args = 1 }
filesize            = { emit = "common:php.filesize",           min_args = 1, max_args = 1 }
filemtime           = { emit = "common:php.filemtime",          min_args = 1, max_args = 1 }
readlink            = { emit = "common:php.readlink",           min_args = 1, max_args = 1 }
mkdir               = { emit = "common:php.mkdir",               min_args = 1, max_args = 4 }
unlink              = { emit = "common:php.unlink",             min_args = 1, max_args = 2 }
sys_get_temp_dir    = { emit = "common:php.sys_get_temp_dir",   min_args = 0, max_args = 0 }
realpath            = { emit = "common:php.realpath",           min_args = 1, max_args = 1 }
copy                = { emit = "common:php.copy",               min_args = 2, max_args = 3 }
rename              = { emit = "common:php.rename",             min_args = 2, max_args = 3 }
rmdir               = { emit = "common:php.rmdir",             min_args = 1, max_args = 2 }
is_readable         = { emit = "common:php.is_readable",        min_args = 1, max_args = 1 }
is_writable         = { emit = "common:php.is_writable",        min_args = 1, max_args = 1 }
is_writeable        = { emit = "common:php.is_writable",        min_args = 1, max_args = 1 }
filetype            = { emit = "common:php.filetype",           min_args = 1, max_args = 1 }
scandir             = { emit = "common:php.scandir",            min_args = 1, max_args = 3 }
tempnam             = { emit = "common:php.tempnam",            min_args = 2, max_args = 2 }
mime_content_type   = { emit = "common:php.mime_content_type", min_args = 1, max_args = 1 }
fileperms           = { emit = "common:php.fileperms",          min_args = 1, max_args = 1 }
disk_free_space     = { emit = "common:php.disk_free_space",   min_args = 1, max_args = 1 }
disk_total_space    = { emit = "common:php.disk_total_space",  min_args = 1, max_args = 1 }

# ── Stream resource I/O (fopen family) ──────────────────────────────────────
fopen               = { emit = "common:php.fopen",             min_args = 2, max_args = 4 }
fwrite              = { emit = "common:php.fwrite",             min_args = 2, max_args = 3 }
fputs               = { emit = "common:php.fwrite",             min_args = 2, max_args = 3 }
fread               = { emit = "common:php.fread",             min_args = 2, max_args = 2 }
fgets               = { emit = "common:php.fgets",             min_args = 1, max_args = 2 }
fgetc               = { emit = "common:php.fgetc",             min_args = 1, max_args = 1 }
feof                = { emit = "common:php.feof",              min_args = 1, max_args = 1 }
ftell               = { emit = "common:php.ftell",             min_args = 1, max_args = 1 }
fseek               = { emit = "common:php.fseek",             min_args = 2, max_args = 3 }
rewind              = { emit = "common:php.rewind",            min_args = 1, max_args = 1 }
fflush              = { emit = "common:php.fflush",            min_args = 1, max_args = 1 }
fclose              = { emit = "common:php.fclose",            min_args = 1, max_args = 1 }
stream_get_contents = { emit = "common:php.stream_get_contents", min_args = 1, max_args = 3 }
stream_get_meta_data = { emit = "common:php.stream_get_meta_data", min_args = 1, max_args = 1 }

# ── Environment / CLI ───────────────────────────────────────────────────────

getenv              = { emit = "host:wasi:cli:getEnv",       min_args = 1, max_args = 1 }
extension_loaded    = { emit = "common:php.extension_loaded", min_args = 1, max_args = 1 }
phpversion          = { emit = "common:php.phpversion",       min_args = 0, max_args = 1 }
phpinfo             = { emit = "common:php.phpinfo",         min_args = 0, max_args = 1 }

# ── PHP error / INI runtime (no-ops for CLI / web compilation) ──────────────
# These are runtime configuration calls that don't affect compiled-bytecode
# behaviour. Stub them so common PHP boilerplate doesn't break at runtime.
error_reporting     = { emit = "common:php.error_reporting",     min_args = 0, max_args = 1 }
ini_set             = { emit = "noop",                        min_args = 2, max_args = 2 }
ini_get             = { emit = "noop",                        min_args = 1, max_args = 1 }
set_error_handler   = { emit = "common:php.set_error_handler",   min_args = 1, max_args = 2 }
restore_error_handler = { emit = "common:php.restore_error_handler", min_args = 0, max_args = 0 }
set_exception_handler = { emit = "common:php.set_exception_handler", min_args = 1, max_args = 1 }
trigger_error       = { emit = "common:php.trigger_error",       min_args = 1, max_args = 2 }
error_get_last      = { emit = "common:php.error_get_last",      min_args = 0, max_args = 0 }
error_clear_last    = { emit = "common:php.error_clear_last",    min_args = 0, max_args = 0 }
register_shutdown_function = { emit = "intrinsic:php_register_shutdown_function", min_args = 1, max_args = 255 }
spl_autoload_register = { emit = "common:php.spl_autoload_register", min_args = 1, max_args = 3 }
spl_autoload_unregister = { emit = "common:php.spl_autoload_unregister", min_args = 1, max_args = 1 }
php_sapi_name       = { emit = "host:node:http:php_sapi_name", min_args = 0, max_args = 0 }
getcwd              = { emit = "host:wasi:cli:cwd",          min_args = 0, max_args = 0 }
gethostname         = { emit = "host:wasi:cli:machineName",  min_args = 0, max_args = 0 }

# ── HTTP / web (vybex --serve) ──────────────────────────────────────────────
header              = { emit = "common:php.header",               min_args = 1, max_args = 3 }
http_response_code  = { emit = "host:node:http:http_response_code", min_args = 0, max_args = 1 }
session_start       = { emit = "common:php.session_start",         min_args = 0, max_args = 0 }
session_unset       = { emit = "common:php.session_unset",         min_args = 0, max_args = 0 }
session_destroy     = { emit = "common:php.session_destroy",       min_args = 0, max_args = 0 }
setcookie           = { emit = "host:node:http:set_cookie",         min_args = 1, max_args = 3 }
setrawcookie        = { emit = "host:node:http:set_cookie",         min_args = 1, max_args = 3 }
headers_sent        = { emit = "host:node:http:headers_sent",      min_args = 0, max_args = 2 }
header_remove       = { emit = "host:node:http:remove_header",     min_args = 0, max_args = 1 }

# ── Miscellaneous ───────────────────────────────────────────────────────────

# `define("NAME", value)` lowers to a global write when the constant
# name is a string literal; `defined("NAME")` probes that same global.
defined             = { emit = "intrinsic:php_defined",      min_args = 1, max_args = 1 }
define              = { emit = "intrinsic:php_define",       min_args = 2, max_args = 3 }
constant            = { emit = "intrinsic:php_constant",     min_args = 1, max_args = 1 }
function_exists     = { emit = "intrinsic:php_function_exists", min_args = 1, max_args = 1 }
class_exists        = { emit = "intrinsic:php_class_exists", min_args = 1, max_args = 2 }

# ── mysqli compatibility (wpdb bootstrap path) ─────────────────────────────
# Routed through the PHP database adapter emitter so WordPress db bootstrap
# can execute without unresolved dynamic `call_ref` fallthroughs.
mysqli_report       = { emit = "common:php.mysqli_report",       min_args = 1, max_args = 1 }
mysqli_connect      = { emit = "common:php.mysqli_connect",      min_args = 1, max_args = 8 }
mysqli_init         = { emit = "common:php.mysqli_init",         min_args = 0, max_args = 0 }
mysqli_real_connect = { emit = "common:php.mysqli_real_connect", min_args = 4, max_args = 8 }
mysqli_connect_errno = { emit = "common:php.mysqli_connect_errno", min_args = 0, max_args = 0 }
mysqli_connect_error = { emit = "common:php.mysqli_connect_error", min_args = 0, max_args = 0 }
mysqli_error        = { emit = "common:php.mysqli_error",        min_args = 1, max_args = 1 }
mysqli_query        = { emit = "common:php.mysqli_query",        min_args = 2, max_args = 2 }
mysqli_prepare      = { emit = "common:php.mysqli_prepare",      min_args = 2, max_args = 2 }
mysqli_select_db    = { emit = "common:php.mysqli_select_db",    min_args = 2, max_args = 2 }
mysqli_set_charset  = { emit = "common:php.mysqli_set_charset",  min_args = 2, max_args = 2 }
mysqli_ping         = { emit = "common:php.mysqli_ping",         min_args = 1, max_args = 1 }
mysqli_errno        = { emit = "common:php.mysqli_errno",        min_args = 1, max_args = 1 }
mysqli_affected_rows = { emit = "common:php.mysqli_affected_rows", min_args = 1, max_args = 1 }
mysqli_insert_id    = { emit = "common:php.mysqli_insert_id",    min_args = 1, max_args = 1 }
mysqli_num_fields   = { emit = "common:php.mysqli_num_fields",   min_args = 1, max_args = 1 }
mysqli_fetch_field  = { emit = "common:php.mysqli_fetch_field",  min_args = 1, max_args = 1 }
mysqli_free_result  = { emit = "common:php.mysqli_free_result",  min_args = 1, max_args = 1 }
mysqli_more_results = { emit = "common:php.mysqli_more_results", min_args = 1, max_args = 1 }
mysqli_next_result  = { emit = "common:php.mysqli_next_result",  min_args = 1, max_args = 1 }
mysqli_close        = { emit = "common:php.mysqli_close",        min_args = 1, max_args = 1 }
mysqli_real_escape_string = { emit = "common:php.mysqli_real_escape_string", min_args = 2, max_args = 2 }
mysqli_character_set_name = { emit = "common:php.mysqli_character_set_name", min_args = 1, max_args = 1 }
mysqli_get_client_info = { emit = "common:php.mysqli_get_client_info", min_args = 0, max_args = 0 }
mysqli_get_server_info = { emit = "common:php.mysqli_get_server_info", min_args = 1, max_args = 1 }
mysqli_fetch_array  = { emit = "common:php.mysqli_fetch_array",  min_args = 1, max_args = 2 }
mysqli_fetch_assoc  = { emit = "common:php.mysqli_fetch_assoc",  min_args = 1, max_args = 1 }
mysqli_fetch_object = { emit = "common:php.mysqli_fetch_object", min_args = 1, max_args = 1 }
mysqli_num_rows     = { emit = "common:php.mysqli_num_rows",     min_args = 1, max_args = 1 }
mysqli_fetch_all    = { emit = "common:php.mysqli_fetch_all",    min_args = 1, max_args = 1 }

# isset / empty / unset — polymorphic primitives shared with the rest of
# the runtime (every language's "is this value defined?" check uses the
# same host-level primitive). `isset($a['key'])` compiles to: first
# evaluate the Index expression (producing the value or Null), then call
# isset_all which checks not-null-not-undefined. Works uniformly across
# Array, Map, and Ordinary backings.
isset               = { emit = "common:php.isset_all",       min_args = 1, max_args = 255 }
empty               = { emit = "common:php.empty",          min_args = 1, max_args = 1 }
serialize           = { emit = "common:php.serialize",      min_args = 1, max_args = 1 }
unserialize         = { emit = "common:php.unserialize",    min_args = 1, max_args = 1 }

# ────────────────────────────────────────────────────────────────────────────
# PHP DateTime / DateInterval — bytecode adapters at
# `emitter/php/datetime_adapter.rs`. The walker rewrites the
# user-facing forms (`new DateTime(...)`, `$dt->format(...)`, etc.)
# into compound names that resolve here to `common:php.<name>` emit
# targets.
# ────────────────────────────────────────────────────────────────────────────

__php_dt_new           = { emit = "common:php.datetime_new",            min_args = 1, max_args = 2 }
__php_dt_imm_new       = { emit = "common:php.datetime_immutable_new",  min_args = 1, max_args = 2 }
__php_datetimezone_new = { emit = "common:php.datetimezone_new",        min_args = 1, max_args = 1 }
__php_dt_get_timezone  = { emit = "common:php.datetime_get_timezone",   min_args = 1, max_args = 1 }
__php_dt_get_offset    = { emit = "common:php.datetime_get_offset",     min_args = 1, max_args = 2 }
__php_dt_set_timezone  = { emit = "common:php.datetime_set_timezone",   min_args = 2, max_args = 2 }
__php_dt_set_date      = { emit = "common:php.datetime_set_date",       min_args = 4, max_args = 4 }
__php_dt_set_time      = { emit = "common:php.datetime_set_time",       min_args = 4, max_args = 4 }
__php_dt_create_from_format = { emit = "common:php.datetime_create_from_format", min_args = 2, max_args = 2 }
__php_dt_imm_create_from_format = { emit = "common:php.datetime_immutable_create_from_format", min_args = 2, max_args = 2 }
__php_dt_format        = { emit = "common:php.datetime_format",         min_args = 2, max_args = 2 }
__php_dt_get_timestamp = { emit = "common:php.datetime_get_timestamp",  min_args = 1, max_args = 1 }
__php_dt_modify        = { emit = "common:php.datetime_modify",         min_args = 2, max_args = 2 }
__php_dt_imm_modify    = { emit = "common:php.datetime_immutable_modify", min_args = 2, max_args = 2 }
__php_dt_diff          = { emit = "common:php.datetime_diff",           min_args = 2, max_args = 2 }
__php_dt_add           = { emit = "common:php.datetime_add",            min_args = 2, max_args = 2 }
__php_dt_sub           = { emit = "common:php.datetime_sub",            min_args = 2, max_args = 2 }
__php_dt_imm_add       = { emit = "common:php.datetime_immutable_add",  min_args = 2, max_args = 2 }
__php_dt_imm_sub       = { emit = "common:php.datetime_immutable_sub",  min_args = 2, max_args = 2 }
__php_dateinterval_components = { emit = "common:php.dateinterval_components", min_args = 6, max_args = 6 }
__php_dt_add_seconds   = { emit = "common:php.datetime_add_seconds", min_args = 2, max_args = 2 }
__php_dt_add_minutes   = { emit = "common:php.datetime_add_minutes", min_args = 2, max_args = 2 }
__php_dt_add_hours     = { emit = "common:php.datetime_add_hours",   min_args = 2, max_args = 2 }
__php_dt_add_days      = { emit = "common:php.datetime_add_days",    min_args = 2, max_args = 2 }
__php_dt_add_weeks     = { emit = "common:php.datetime_add_weeks",   min_args = 2, max_args = 2 }
__php_dt_add_months    = { emit = "common:php.datetime_add_months",  min_args = 2, max_args = 2 }
__php_dt_add_years     = { emit = "common:php.datetime_add_years",   min_args = 2, max_args = 2 }
# `strtotime("+N month", $base)` / `"+N year"` calendar-shift adapter
# emitted by the walker. Args: (base_secs, n, is_year_bool). Returns
# new seconds epoch.
__php_strtotime_rel_calendar = { emit = "common:php.strtotime_rel_calendar", min_args = 3, max_args = 3 }
# Walker-emitted helpers for `preg_match_all($p,$s,$m)` / `preg_match($p,$s,$m)`
# 3-arg form. The walker rewrites the call to `$m = __preg_*_groups($p,$s)`.
__preg_match_all_groups = { emit = "common:php.preg_match_all_groups", min_args = 2, max_args = 2 }
__preg_match_groups     = { emit = "common:php.preg_match_groups",     min_args = 2, max_args = 2 }

# PHP Fiber adapters (PHP 8.1) — leverage the VM's WASM stack-switching
# opcodes (`CONT_NEW`, `RESUME`, `SUSPEND`) directly. No host fns; no
# polyfills. Walker rewrites:
#   `new Fiber($cb)`     → __php_fiber_new($cb)
#   `Fiber::suspend(v)`  → __php_fiber_suspend(v)
#   `$f->start(args...)` → __php_fiber_start($f, args...)
#   `$f->resume(v)`      → __php_fiber_resume($f, v)
#   `$f->getReturn()`    → __php_fiber_get_return($f)
# SPL data-structure constructors (walker rewrites `new SplX()` → these).
__spl_new_splstack             = { emit = "common:php.spl_splstack",             min_args = 0, max_args = 0 }
__spl_new_splqueue             = { emit = "common:php.spl_splqueue",             min_args = 0, max_args = 0 }
__spl_new_spldoublylinkedlist  = { emit = "common:php.spl_spldoublylinkedlist",  min_args = 0, max_args = 0 }
__spl_new_splminheap           = { emit = "common:php.spl_splminheap",           min_args = 0, max_args = 0 }
__spl_new_splmaxheap           = { emit = "common:php.spl_splmaxheap",           min_args = 0, max_args = 0 }
__spl_new_splpriorityqueue     = { emit = "common:php.spl_splpriorityqueue",     min_args = 0, max_args = 0 }
__spl_new_splfixedarray        = { emit = "common:php.spl_splfixedarray",        min_args = 0, max_args = 1 }
__spl_fixedarray_from_array     = { emit = "common:php.spl_fixedarray_fromarray",  min_args = 1, max_args = 1 }
__spl_new_splobjectstorage     = { emit = "common:php.spl_splobjectstorage",     min_args = 0, max_args = 0 }
__spl_new_weakmap              = { emit = "common:php.spl_weakmap",              min_args = 0, max_args = 0 }
__refl_class                   = { emit = "common:php.refl_class",               min_args = 1, max_args = 8 }
__refl_method                  = { emit = "common:php.refl_method",              min_args = 2, max_args = 5 }
__refl_property                = { emit = "common:php.refl_property",            min_args = 2, max_args = 2 }
__refl_function                = { emit = "common:php.refl_function",            min_args = 1, max_args = 3 }
__weak_ref_create              = { emit = "common:php.weak_ref_create",          min_args = 1, max_args = 1 }
__php_array_union              = { emit = "common:php.array_union",              min_args = 2, max_args = 2 }
__php_obj_to_array             = { emit = "common:php.obj_to_array",             min_args = 1, max_args = 1 }
__php_array_to_object          = { emit = "common:php.array_to_object",          min_args = 1, max_args = 1 }
__php_fiber_new           = { emit = "common:php.fiber_new",           min_args = 1, max_args = 1 }
__php_fiber_suspend       = { emit = "common:php.fiber_suspend",       min_args = 0, max_args = 1 }
__php_fiber_start         = { emit = "common:php.fiber_start",         min_args = 1, max_args = 16 }
__php_fiber_resume        = { emit = "common:php.fiber_resume",        min_args = 1, max_args = 2 }
__php_fiber_get_return    = { emit = "common:php.fiber_get_return",    min_args = 1, max_args = 1 }
__php_fiber_is_started    = { emit = "common:php.fiber_is_started",    min_args = 1, max_args = 1 }
__php_fiber_is_suspended  = { emit = "common:php.fiber_is_suspended",  min_args = 1, max_args = 1 }
__php_fiber_is_running    = { emit = "common:php.fiber_is_running",    min_args = 1, max_args = 1 }
__php_fiber_is_terminated = { emit = "common:php.fiber_is_terminated", min_args = 1, max_args = 1 }

# ────────────────────────────────────────────────────────────────────────────
# VALUE METHODS — instance method calls on intrinsic types
# ────────────────────────────────────────────────────────────────────────────
#
# PHP's standard library is mostly bare functions, not methods. The handful
# of object-oriented APIs (DateTime, ArrayObject, SplStack, …) are wired
# up as instance methods here. The compiler routes them through the
# value_methods table the same way JS / VB do.

[known_types]
PDO = ["common", "php.pdo_new"]
mysqli = ["common", "php.mysqli_init"]

[value_methods]

toFixed          = { emit = "host:ecma:number:toFixed",           min_args = 0, max_args = 1 }

query            = { emit = "common:php.pdo_query",              min_args = 1, max_args = 1 }
exec             = { emit = "common:php.pdo_exec",               min_args = 1, max_args = 1 }
prepare          = { emit = "common:php.db_prepare",             min_args = 1, max_args = 1 }
setAttribute     = { emit = "common:php.pdo_set_attribute",      min_args = 2, max_args = 2 }
beginTransaction = { emit = "common:php.pdo_begin_transaction",  min_args = 0, max_args = 0 }
commit           = { emit = "common:php.pdo_commit",             min_args = 0, max_args = 0 }
rollBack         = { emit = "common:php.pdo_rollback",           min_args = 0, max_args = 0 }
execute          = { emit = "common:php.pdo_statement_execute",  min_args = 0, max_args = 1 }
bindParam        = { emit = "common:php.pdo_statement_bind_param", min_args = 2, max_args = 5 }
bindValue        = { emit = "common:php.pdo_statement_bind_value", min_args = 2, max_args = 4 }
fetch            = { emit = "common:php.pdo_statement_fetch",    min_args = 0, max_args = 1 }
fetchAll         = { emit = "common:php.pdo_statement_fetch_all", min_args = 0, max_args = 1 }
fetchColumn      = { emit = "common:php.pdo_statement_fetch_column", min_args = 0, max_args = 1 }
rowCount         = { emit = "common:php.pdo_statement_row_count",  min_args = 0, max_args = 0 }
columnCount      = { emit = "common:php.pdo_statement_column_count", min_args = 0, max_args = 0 }
getAttribute     = { emit = "common:php.pdo_get_attribute",       min_args = 1, max_args = 1 }
quote            = { emit = "common:php.pdo_quote",               min_args = 1, max_args = 2 }
errorCode        = { emit = "common:php.pdo_error_code",          min_args = 0, max_args = 0 }
errorInfo        = { emit = "common:php.pdo_error_info",           min_args = 0, max_args = 0 }
lastInsertId     = { emit = "common:php.pdo_last_insert_id",      min_args = 0, max_args = 1 }
attr_get         = { emit = "common:php.mysqli_stmt_attr_get",    min_args = 1, max_args = 1 }

# ── mysqli_stmt OO methods (thin over the shared wasi:sql db_adapter) ──
bind_param       = { emit = "common:php.mysqli_stmt_true",       min_args = 1, max_args = 255 }
bind_result      = { emit = "common:php.mysqli_stmt_true",       min_args = 1, max_args = 255 }
store_result     = { emit = "common:php.mysqli_stmt_true",       min_args = 0, max_args = 0 }
free_result      = { emit = "common:php.mysqli_stmt_true",       min_args = 0, max_args = 0 }
reset            = { emit = "common:php.mysqli_stmt_true",       min_args = 0, max_args = 0 }
close            = { emit = "common:php.mysqli_stmt_true",       min_args = 0, max_args = 0 }
send_long_data   = { emit = "common:php.mysqli_stmt_true",       min_args = 2, max_args = 2 }
data_seek        = { emit = "common:php.mysqli_stmt_true",       min_args = 1, max_args = 1 }
get_warnings     = { emit = "common:php.mysqli_stmt_false",      min_args = 0, max_args = 0 }
more_results     = { emit = "common:php.mysqli_stmt_false",      min_args = 0, max_args = 0 }
next_result      = { emit = "common:php.mysqli_stmt_false",      min_args = 0, max_args = 0 }
get_result       = { emit = "common:php.mysqli_stmt_get_result", min_args = 0, max_args = 0 }
fetch_assoc      = { emit = "common:php.mysqli_fetch_assoc",     min_args = 0, max_args = 0 }
fetch_row        = { emit = "common:php.mysqli_fetch_array",     min_args = 0, max_args = 0 }
fetch_object     = { emit = "common:php.mysqli_fetch_object",    min_args = 0, max_args = 1 }
fetch_all        = { emit = "common:php.mysqli_fetch_all",       min_args = 0, max_args = 1 }

# ────────────────────────────────────────────────────────────────────────────
# ARRAY HIGHER-ORDER METHODS — same dispatch as JS profile.
#
# Walker rewrites of `array_map`/`array_filter`/`array_reduce`/etc. into
# JS-shape `$arr.method(fn, ...)` resolve through this table to the
# language-agnostic loop helpers in compiler_common.
# ────────────────────────────────────────────────────────────────────────────

# NB: only the higher-order names that PHP code actually reaches via
# `$arr.method(...)` go here. PHP function names like `find` are
# popular as user class methods (`$repo->find(1)`); listing them
# here would intercept those user methods. Keep this table minimal.
[array_methods]
map      = "__array_map"
filter   = "__array_filter"
reduce   = "__array_reduce"



# ────────────────────────────────────────────────────────────────────────────
# NAMESPACE CONSTANTS — property access, NOT function call.
#
# PHP `M_PI` / `M_E` / etc. are rewritten by the walker into JS-shaped
# Member access (`Math.PI`, `Math.E`, ...). The compiler's lookup_constant
# resolves these here, emitting the f64 literal directly — no host call,
# no intrinsic. Mirrors the JS profile's [namespace_constants] table.
# ────────────────────────────────────────────────────────────────────────────

[namespace_constants]
"PHP_VERSION"           = "8.0.0"
"PHP_VERSION_ID"        = 80000
"Math.PI"               = 3.141592653589793
"Math.E"                = 2.718281828459045
"Math.LN2"              = 0.6931471805599453
"Math.LN10"             = 2.302585092994046
"Math.LOG2E"            = 1.4426950408889634
"Math.LOG10E"           = 0.4342944819032518
"Math.SQRT2"            = 1.4142135623730951
"Math.SQRT1_2"          = 0.7071067811865476
"Number.MAX_SAFE_INTEGER" = 9007199254740991
"Number.MIN_SAFE_INTEGER" = -9007199254740991
"Number.EPSILON"        = 2.220446049250313e-16
"Number.MAX_VALUE"      = 1.7976931348623157e308
"Number.MIN_VALUE"      = 5e-324
"Number.POSITIVE_INFINITY" = "Infinity"
"Number.NEGATIVE_INFINITY" = "-Infinity"
"Number.NaN"            = "NaN"
"PDO.ATTR_ERRMODE"      = 3
"PDO.ERRMODE_EXCEPTION" = 2
"PDO.PARAM_INT"        = 1
"PDO.PARAM_STR"        = 2
"PDO.FETCH_ASSOC"       = 2
"PDO.FETCH_COLUMN"      = 7

# ────────────────────────────────────────────────────────────────────────────
# NAMESPACES — none. PHP namespaces are flattened by the walker.
# ────────────────────────────────────────────────────────────────────────────

[namespaces]
use_dotnet = false
roots = []
extra_imports = []

# ────────────────────────────────────────────────────────────────────────────
# ESM DEFAULTS — Component Model host packages.
#
# Any qualified call whose first segment matches one of these resolves
# directly to a host call, no profile builtin entry required.
# E.g. `\Vybe\Http\Response\set_status(404)` →
#   host call `(module="vybe:http/response", name="set_status")`.
# ────────────────────────────────────────────────────────────────────────────

[[esm_default]]
kind = "package-root"
prefix = "vybe"
module_root = "vybe:"

[[esm_default]]
kind = "package-root"
prefix = "wasi"
module_root = "wasi:"

[[esm_default]]
kind = "package-root"
prefix = "wasm"
module_root = "wasm:"
