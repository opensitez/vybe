use crate::helpers;

macro_rules! cobol_test {
    ($name:ident, $src:expr, $expected:expr) => {
        #[test]
        fn $name() {
            let out = crate::helpers::run_prints($src);
            assert_eq!(out, $expected);
        }
    };
}

cobol_test!(
    test_search_linear_no_match,
    "IDENTIFICATION DIVISION. PROGRAM-ID. T. DATA DIVISION. WORKING-STORAGE SECTION. 01 TBL. 05 EL OCCURS 3 TIMES INDEXED BY I PIC X. PROCEDURE DIVISION. MOVE 'A' TO EL(1). MOVE 'B' TO EL(2). MOVE 'C' TO EL(3). SET I TO 1. SEARCH EL AT END DISPLAY 'N' WHEN EL(I) = 'Z' DISPLAY 'Y' END-SEARCH. STOP RUN.",
    vec!["N"]
);
cobol_test!(
    test_search_linear_multiple_actions,
    "IDENTIFICATION DIVISION. PROGRAM-ID. T. DATA DIVISION. WORKING-STORAGE SECTION. 01 TBL. 05 EL OCCURS 3 TIMES INDEXED BY I PIC X. PROCEDURE DIVISION. MOVE 'A' TO EL(1). MOVE 'B' TO EL(2). MOVE 'C' TO EL(3). SET I TO 1. SEARCH EL WHEN EL(I) = 'B' DISPLAY '1' DISPLAY '2' END-SEARCH. STOP RUN.",
    vec!["1", "2"]
);
cobol_test!(
    test_search_all_compound_and,
    "IDENTIFICATION DIVISION. PROGRAM-ID. T. DATA DIVISION. WORKING-STORAGE SECTION. 01 TBL. 05 EL OCCURS 3 TIMES ASCENDING K1 K2 INDEXED BY I. 10 K1 PIC 9. 10 K2 PIC X. PROCEDURE DIVISION. MOVE 1 TO K1(1). MOVE 'A' TO K2(1). MOVE 2 TO K1(2). MOVE 'B' TO K2(2). SEARCH ALL EL WHEN K1(I) = 2 AND K2(I) = 'B' DISPLAY 'Y' END-SEARCH. STOP RUN.",
    vec!["Y"]
);
cobol_test!(
    test_search_all_compound_not_found,
    "IDENTIFICATION DIVISION. PROGRAM-ID. T. DATA DIVISION. WORKING-STORAGE SECTION. 01 TBL. 05 EL OCCURS 3 TIMES ASCENDING K1 K2 INDEXED BY I. 10 K1 PIC 9. 10 K2 PIC X. PROCEDURE DIVISION. MOVE 1 TO K1(1). MOVE 'A' TO K2(1). MOVE 2 TO K1(2). MOVE 'B' TO K2(2). SEARCH ALL EL AT END DISPLAY 'N' WHEN K1(I) = 2 AND K2(I) = 'C' DISPLAY 'Y' END-SEARCH. STOP RUN.",
    vec!["N"]
);
cobol_test!(
    test_search_all_descending,
    "IDENTIFICATION DIVISION. PROGRAM-ID. T. DATA DIVISION. WORKING-STORAGE SECTION. 01 TBL. 05 EL OCCURS 3 TIMES DESCENDING K INDEXED BY I. 10 K PIC 9. PROCEDURE DIVISION. MOVE 3 TO K(1). MOVE 2 TO K(2). MOVE 1 TO K(3). SEARCH ALL EL WHEN K(I) = 2 DISPLAY 'Y' END-SEARCH. STOP RUN.",
    vec!["Y"]
);
cobol_test!(
    test_search_linear_varying_external,
    "IDENTIFICATION DIVISION. PROGRAM-ID. T. DATA DIVISION. WORKING-STORAGE SECTION. 01 TBL. 05 EL OCCURS 3 TIMES INDEXED BY I PIC X. 01 IDX PIC 9 VALUE 1. PROCEDURE DIVISION. MOVE 'A' TO EL(1). MOVE 'B' TO EL(2). MOVE 'C' TO EL(3). SET I TO 1. SEARCH EL VARYING IDX WHEN EL(I) = 'C' DISPLAY IDX END-SEARCH. STOP RUN.",
    vec!["3"]
);
cobol_test!(
    test_search_linear_varying_index,
    "IDENTIFICATION DIVISION. PROGRAM-ID. T. DATA DIVISION. WORKING-STORAGE SECTION. 01 TBL. 05 EL OCCURS 3 TIMES INDEXED BY I J PIC X. PROCEDURE DIVISION. MOVE 'A' TO EL(1). MOVE 'B' TO EL(2). MOVE 'C' TO EL(3). SET I TO 1. SET J TO 1. SEARCH EL VARYING J WHEN EL(I) = 'B' DISPLAY J END-SEARCH. STOP RUN.",
    vec!["2"]
);
cobol_test!(
    test_search_all_condition_name,
    "IDENTIFICATION DIVISION. PROGRAM-ID. T. DATA DIVISION. WORKING-STORAGE SECTION. 01 TBL. 05 EL OCCURS 3 TIMES ASCENDING K INDEXED BY I. 10 K PIC 9. 88 IS-TWO VALUE 2. PROCEDURE DIVISION. MOVE 1 TO K(1). MOVE 2 TO K(2). MOVE 3 TO K(3). SEARCH ALL EL WHEN IS-TWO(I) DISPLAY 'Y' END-SEARCH. STOP RUN.",
    vec!["Y"]
);
cobol_test!(
    test_search_linear_condition_name,
    "IDENTIFICATION DIVISION. PROGRAM-ID. T. DATA DIVISION. WORKING-STORAGE SECTION. 01 TBL. 05 EL OCCURS 3 TIMES INDEXED BY I. 10 K PIC 9. 88 IS-TWO VALUE 2. PROCEDURE DIVISION. MOVE 1 TO K(1). MOVE 2 TO K(2). MOVE 3 TO K(3). SET I TO 1. SEARCH EL WHEN IS-TWO(I) DISPLAY 'Y' END-SEARCH. STOP RUN.",
    vec!["Y"]
);
cobol_test!(
    test_search_all_first_match,
    "IDENTIFICATION DIVISION. PROGRAM-ID. T. DATA DIVISION. WORKING-STORAGE SECTION. 01 TBL. 05 EL OCCURS 3 TIMES ASCENDING K INDEXED BY I. 10 K PIC 9. PROCEDURE DIVISION. MOVE 1 TO K(1). MOVE 1 TO K(2). MOVE 2 TO K(3). SEARCH ALL EL WHEN K(I) = 1 DISPLAY I END-SEARCH. STOP RUN.",
    vec!["1"]
);
cobol_test!(
    test_search_all_with_varying_index,
    "IDENTIFICATION DIVISION. PROGRAM-ID. T. DATA DIVISION. WORKING-STORAGE SECTION. 01 TBL. 05 EL OCCURS 4 TIMES ASCENDING K INDEXED BY I. 10 K PIC 9. 01 POS PIC 9 VALUE 1. PROCEDURE DIVISION. MOVE 1 TO K(1). MOVE 2 TO K(2). MOVE 3 TO K(3). MOVE 4 TO K(4). SEARCH ALL EL VARYING POS WHEN K(I) = 3 DISPLAY POS END-SEARCH STOP RUN.",
    vec!["3"]
);
cobol_test!(
    test_search_all_not_found_returns_end,
    "IDENTIFICATION DIVISION. PROGRAM-ID. T. DATA DIVISION. WORKING-STORAGE SECTION. 01 TBL. 05 EL OCCURS 2 TIMES ASCENDING K INDEXED BY I. 10 K PIC 9. PROCEDURE DIVISION. MOVE 1 TO K(1). MOVE 2 TO K(2). SEARCH ALL EL AT END DISPLAY 'NOT' WHEN K(I) = 9 DISPLAY 'YES' END-SEARCH DISPLAY 'DONE'. STOP RUN.",
    vec!["NOT", "DONE"]
);
cobol_test!(
    test_search_linear_then_evaluate_display,
    "IDENTIFICATION DIVISION. PROGRAM-ID. T. DATA DIVISION. WORKING-STORAGE SECTION. 01 TBL. 05 EL OCCURS 3 TIMES INDEXED BY I PIC X. 01 FLAG PIC X VALUE 'N'. PROCEDURE DIVISION. MOVE 'A' TO EL(1). MOVE 'B' TO EL(2). MOVE 'C' TO EL(3). SET I TO 1. SEARCH EL WHEN EL(I) = 'B' MOVE 'Y' TO FLAG END-SEARCH. EVALUATE FLAG WHEN 'Y' DISPLAY 'HIT' WHEN OTHER DISPLAY 'MISS' END-EVALUATE STOP RUN.",
    vec!["HIT"]
);
