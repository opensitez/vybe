*> vybe-test: cobol/arrays_tables_indexing/table_move_zeros_compiles
*> origin: languages/cobol/tests/cobol/test_arrays_tables_indexing.rs
IDENTIFICATION DIVISION.
PROGRAM-ID. T.
DATA DIVISION.
WORKING-STORAGE SECTION.
01 T PIC 9(2) OCCURS 4 TIMES VALUE 12.
01 WS-VYBE-L PIC X(256).
PROCEDURE DIVISION.
    MOVE ZEROS TO T.
    DISPLAY T(1).
    MOVE SPACES TO WS-VYBE-L
    STRING T(1) DELIMITED SIZE INTO WS-VYBE-L
    IF WS-VYBE-L NOT = "00"
        DISPLAY "FAIL: want [00] got [" WS-VYBE-L "]"
        MOVE 1 TO RETURN-CODE
        RAISE EXCEPTION EC-PROGRAM
    END-IF.
    DISPLAY T(2).
    MOVE SPACES TO WS-VYBE-L
    STRING T(2) DELIMITED SIZE INTO WS-VYBE-L
    IF WS-VYBE-L NOT = "00"
        DISPLAY "FAIL: want [00] got [" WS-VYBE-L "]"
        MOVE 1 TO RETURN-CODE
        RAISE EXCEPTION EC-PROGRAM
    END-IF.
    DISPLAY T(3).
    MOVE SPACES TO WS-VYBE-L
    STRING T(3) DELIMITED SIZE INTO WS-VYBE-L
    IF WS-VYBE-L NOT = "00"
        DISPLAY "FAIL: want [00] got [" WS-VYBE-L "]"
        MOVE 1 TO RETURN-CODE
        RAISE EXCEPTION EC-PROGRAM
    END-IF.
    DISPLAY T(4).
    MOVE SPACES TO WS-VYBE-L
    STRING T(4) DELIMITED SIZE INTO WS-VYBE-L
    IF WS-VYBE-L NOT = "00"
        DISPLAY "FAIL: want [00] got [" WS-VYBE-L "]"
        MOVE 1 TO RETURN-CODE
        RAISE EXCEPTION EC-PROGRAM
    END-IF.
    STOP RUN.

