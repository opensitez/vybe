*> vybe-test: cobol/alphabet_clause/alphabet_clause_custom_order_runtime
*> origin: languages/cobol/tests/cobol/test_alphabet_clause.rs

IDENTIFICATION DIVISION.
PROGRAM-ID. ALPHA19.
ENVIRONMENT DIVISION.
CONFIGURATION SECTION.
SPECIAL-NAMES.
    ALPHABET PRIORITY-ALPHA IS "C" "B" "A" "D" THRU "F".
    COLLATING SEQUENCE IS PRIORITY-ALPHA.
DATA DIVISION.
WORKING-STORAGE SECTION.
01 WS-VYBE-L PIC X(256).
01 FIRST  PIC X VALUE "C".
01 SECOND PIC X VALUE "A".
PROCEDURE DIVISION.
    IF FIRST > SECOND
        DISPLAY "C-A"
    ELSE
        DISPLAY "NOT-C-A"
    END-IF.
    MOVE SPACES TO WS-VYBE-L
    STRING "C-A" DELIMITED SIZE INTO WS-VYBE-L
    IF WS-VYBE-L NOT = "C-A"
        DISPLAY "FAIL: want [C-A] got [" WS-VYBE-L "]"
        MOVE 1 TO RETURN-CODE
        RAISE EXCEPTION EC-PROGRAM
    END-IF.
    STOP RUN.

