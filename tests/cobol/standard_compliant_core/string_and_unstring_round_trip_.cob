*> vybe-test: cobol/standard_compliant_core/string_and_unstring_round_trip_fields
*> origin: languages/cobol/tests/cobol/test_standard_compliant_core.rs
IDENTIFICATION DIVISION.
PROGRAM-ID. T.
DATA DIVISION.
WORKING-STORAGE SECTION.
01 WS-A PIC X(3) VALUE "ONE".
01 WS-B PIC X(3) VALUE "TWO".
01 WS-COMBINED PIC X(8) VALUE SPACES.
01 WS-R1 PIC X(3) VALUE SPACES.
01 WS-R2 PIC X(3) VALUE SPACES.
01 WS-VYBE-L PIC X(256).
PROCEDURE DIVISION.

    STRING WS-A DELIMITED BY SIZE
           "," DELIMITED BY SIZE
           WS-B DELIMITED BY SIZE
           INTO WS-COMBINED.
    UNSTRING WS-COMBINED DELIMITED BY "," INTO WS-R1 WS-R2.
    DISPLAY WS-R1.
    MOVE SPACES TO WS-VYBE-L
    STRING WS-R1 DELIMITED SIZE INTO WS-VYBE-L
    IF WS-VYBE-L NOT = "ONE"
        DISPLAY "FAIL: want [ONE] got [" WS-VYBE-L "]"
        MOVE 1 TO RETURN-CODE
        RAISE EXCEPTION EC-PROGRAM
    END-IF.
    DISPLAY WS-R2.
    MOVE SPACES TO WS-VYBE-L
    STRING WS-R2 DELIMITED SIZE INTO WS-VYBE-L
    IF WS-VYBE-L NOT = "TWO"
        DISPLAY "FAIL: want [TWO] got [" WS-VYBE-L "]"
        MOVE 1 TO RETURN-CODE
        RAISE EXCEPTION EC-PROGRAM
    END-IF.
    STOP RUN.

