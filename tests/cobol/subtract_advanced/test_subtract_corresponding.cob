*> vybe-test: cobol/subtract_advanced/test_subtract_corresponding
*> origin: languages/cobol/tests/cobol/test_subtract_advanced.rs
IDENTIFICATION DIVISION.
PROGRAM-ID. T.
DATA DIVISION.
WORKING-STORAGE SECTION.
01 WS-G1.
   05 WS-X PIC 9(3) VALUE 5.
   05 WS-Y PIC 9(3) VALUE 10.
01 WS-G2.
   05 WS-X PIC 9(3) VALUE 20.
   05 WS-Y PIC 9(3) VALUE 30.
01 WS-VYBE-L PIC X(256).
PROCEDURE DIVISION.

    SUBTRACT CORRESPONDING WS-G1 FROM WS-G2.
    DISPLAY WS-X.
    MOVE SPACES TO WS-VYBE-L
    STRING WS-X DELIMITED SIZE INTO WS-VYBE-L
    IF WS-VYBE-L NOT = "015"
        DISPLAY "FAIL: want [015] got [" WS-VYBE-L "]"
        MOVE 1 TO RETURN-CODE
        RAISE EXCEPTION EC-PROGRAM
    END-IF.
    DISPLAY WS-Y.
    MOVE SPACES TO WS-VYBE-L
    STRING WS-Y DELIMITED SIZE INTO WS-VYBE-L
    IF WS-VYBE-L NOT = "020"
        DISPLAY "FAIL: want [020] got [" WS-VYBE-L "]"
        MOVE 1 TO RETURN-CODE
        RAISE EXCEPTION EC-PROGRAM
    END-IF.
    STOP RUN.

