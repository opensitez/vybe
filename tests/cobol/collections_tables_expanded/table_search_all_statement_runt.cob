*> vybe-test: cobol/collections_tables_expanded/table_search_all_statement_runtime
*> origin: languages/cobol/tests/cobol/test_collections_tables_expanded.rs
IDENTIFICATION DIVISION.
PROGRAM-ID. T.
DATA DIVISION.
WORKING-STORAGE SECTION.
01 WS-TABLE.
   05 WS-ENTRY OCCURS 10 TIMES ASCENDING KEY IS WS-KEY INDEXED BY WS-IDX.
      10 WS-KEY PIC 9(3).
01 WS-VYBE-L PIC X(256).
PROCEDURE DIVISION.
    MOVE 10 TO WS-KEY(1).
    MOVE 20 TO WS-KEY(2).
    MOVE 30 TO WS-KEY(3).
    SEARCH ALL WS-ENTRY
        WHEN WS-KEY(WS-IDX) = 20 DISPLAY "FOUND"
    END-SEARCH.
    MOVE SPACES TO WS-VYBE-L
    STRING "FOUND" DELIMITED SIZE INTO WS-VYBE-L
    IF WS-VYBE-L NOT = "FOUND"
        DISPLAY "FAIL: want [FOUND] got [" WS-VYBE-L "]"
        MOVE 1 TO RETURN-CODE
        RAISE EXCEPTION EC-PROGRAM
    END-IF.
    STOP RUN.

