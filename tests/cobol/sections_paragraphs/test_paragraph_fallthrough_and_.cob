*> vybe-test: cobol/sections_paragraphs/test_paragraph_fallthrough_and_through
*> origin: languages/cobol/tests/cobol/test_sections_paragraphs.rs
IDENTIFICATION DIVISION.
PROGRAM-ID. T.
DATA DIVISION.
WORKING-STORAGE SECTION.
01 WS-VYBE-L PIC X(256).
PROCEDURE DIVISION.

    PERFORM P1 THRU P4.
    STOP RUN.
P1.
    DISPLAY "PARA1".
    MOVE SPACES TO WS-VYBE-L
    STRING "PARA1" DELIMITED SIZE INTO WS-VYBE-L
    IF WS-VYBE-L NOT = "PARA1"
        DISPLAY "FAIL: want [PARA1] got [" WS-VYBE-L "]"
        MOVE 1 TO RETURN-CODE
        RAISE EXCEPTION EC-PROGRAM
    END-IF.
P2.
    DISPLAY "PARA2".
    MOVE SPACES TO WS-VYBE-L
    STRING "PARA2" DELIMITED SIZE INTO WS-VYBE-L
    IF WS-VYBE-L NOT = "PARA2"
        DISPLAY "FAIL: want [PARA2] got [" WS-VYBE-L "]"
        MOVE 1 TO RETURN-CODE
        RAISE EXCEPTION EC-PROGRAM
    END-IF.
P3.
    DISPLAY "PARA3".
    MOVE SPACES TO WS-VYBE-L
    STRING "PARA3" DELIMITED SIZE INTO WS-VYBE-L
    IF WS-VYBE-L NOT = "PARA3"
        DISPLAY "FAIL: want [PARA3] got [" WS-VYBE-L "]"
        MOVE 1 TO RETURN-CODE
        RAISE EXCEPTION EC-PROGRAM
    END-IF.
P4.
    DISPLAY "PARA4".
    MOVE SPACES TO WS-VYBE-L
    STRING "PARA4" DELIMITED SIZE INTO WS-VYBE-L
    IF WS-VYBE-L NOT = "PARA4"
        DISPLAY "FAIL: want [PARA4] got [" WS-VYBE-L "]"
        MOVE 1 TO RETURN-CODE
        RAISE EXCEPTION EC-PROGRAM
    END-IF.
    STOP RUN.

