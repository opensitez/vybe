*> vybe-test: cobol/usage_clause/test_usage_clauses
*> origin: languages/cobol/tests/cobol/test_usage_clause.rs
IDENTIFICATION DIVISION.
PROGRAM-ID. T.
DATA DIVISION.
WORKING-STORAGE SECTION.
01 WS-A PIC 9(4) USAGE IS DISPLAY.
01 WS-B PIC 9(4) USAGE IS BINARY.
01 WS-C PIC 9(4) USAGE IS PACKED-DECIMAL.
01 WS-D PIC 9(4) USAGE IS COMPUTATIONAL.
01 WS-E PIC 9(4) USAGE IS COMP.
01 WS-F PIC 9(4) USAGE IS COMP-3.
01 WS-VYBE-L PIC X(256).
PROCEDURE DIVISION.

    ADD 5 TO WS-A.
    ADD 5 TO WS-B.
    ADD 5 TO WS-C.
    ADD 5 TO WS-D.
    ADD 5 TO WS-E.
    ADD 5 TO WS-F.
    DISPLAY WS-A.
    MOVE SPACES TO WS-VYBE-L
    STRING WS-A DELIMITED SIZE INTO WS-VYBE-L
    IF WS-VYBE-L NOT = "5"
        DISPLAY "FAIL: want [5] got [" WS-VYBE-L "]"
        MOVE 1 TO RETURN-CODE
        RAISE EXCEPTION EC-PROGRAM
    END-IF.
    DISPLAY WS-B.
    MOVE SPACES TO WS-VYBE-L
    STRING WS-B DELIMITED SIZE INTO WS-VYBE-L
    IF WS-VYBE-L NOT = "5"
        DISPLAY "FAIL: want [5] got [" WS-VYBE-L "]"
        MOVE 1 TO RETURN-CODE
        RAISE EXCEPTION EC-PROGRAM
    END-IF.
    DISPLAY WS-C.
    MOVE SPACES TO WS-VYBE-L
    STRING WS-C DELIMITED SIZE INTO WS-VYBE-L
    IF WS-VYBE-L NOT = "5"
        DISPLAY "FAIL: want [5] got [" WS-VYBE-L "]"
        MOVE 1 TO RETURN-CODE
        RAISE EXCEPTION EC-PROGRAM
    END-IF.
    DISPLAY WS-D.
    MOVE SPACES TO WS-VYBE-L
    STRING WS-D DELIMITED SIZE INTO WS-VYBE-L
    IF WS-VYBE-L NOT = "5"
        DISPLAY "FAIL: want [5] got [" WS-VYBE-L "]"
        MOVE 1 TO RETURN-CODE
        RAISE EXCEPTION EC-PROGRAM
    END-IF.
    DISPLAY WS-E.
    MOVE SPACES TO WS-VYBE-L
    STRING WS-E DELIMITED SIZE INTO WS-VYBE-L
    IF WS-VYBE-L NOT = "5"
        DISPLAY "FAIL: want [5] got [" WS-VYBE-L "]"
        MOVE 1 TO RETURN-CODE
        RAISE EXCEPTION EC-PROGRAM
    END-IF.
    DISPLAY WS-F.
    MOVE SPACES TO WS-VYBE-L
    STRING WS-F DELIMITED SIZE INTO WS-VYBE-L
    IF WS-VYBE-L NOT = "5"
        DISPLAY "FAIL: want [5] got [" WS-VYBE-L "]"
        MOVE 1 TO RETURN-CODE
        RAISE EXCEPTION EC-PROGRAM
    END-IF.
    STOP RUN.

