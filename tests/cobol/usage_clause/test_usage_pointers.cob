*> vybe-test: cobol/usage_clause/test_usage_pointers
*> origin: languages/cobol/tests/cobol/test_usage_clause.rs
IDENTIFICATION DIVISION.
PROGRAM-ID. T.
DATA DIVISION.
WORKING-STORAGE SECTION.
01 WS-PTR USAGE IS POINTER.
01 WS-PPTR USAGE IS PROCEDURE-POINTER.
01 WS-FLAG PIC X VALUE 'N'.
01 WS-VYBE-L PIC X(256).
PROCEDURE DIVISION.

    SET WS-PTR TO NULL.
    IF WS-PTR = NULL
        MOVE 'Y' TO WS-FLAG
    END-IF.
    DISPLAY WS-FLAG.
    MOVE SPACES TO WS-VYBE-L
    STRING WS-FLAG DELIMITED SIZE INTO WS-VYBE-L
    IF WS-VYBE-L NOT = "Y"
        DISPLAY "FAIL: want [Y] got [" WS-VYBE-L "]"
        MOVE 1 TO RETURN-CODE
        RAISE EXCEPTION EC-PROGRAM
    END-IF.
    STOP RUN.

