*> vybe-test: cobol/category_data_division_global_clause/test_global_parse_multiple_global_items
*> origin: languages/cobol/tests/cobol/test_category_data_division_global_clause.rs
IDENTIFICATION DIVISION. PROGRAM-ID. T. DATA DIVISION. WORKING-STORAGE SECTION.
01 WS-VYBE-L PIC X(256). 01 G1 PIC 9 VALUE 1 IS GLOBAL. 01 G2 PIC 9 VALUE 10 IS GLOBAL. PROCEDURE DIVISION. DISPLAY G1.
    MOVE SPACES TO WS-VYBE-L
    STRING G1 DELIMITED SIZE INTO WS-VYBE-L
    IF WS-VYBE-L NOT = "1"
        DISPLAY "FAIL: want [1] got [" WS-VYBE-L "]"
        MOVE 1 TO RETURN-CODE
        RAISE EXCEPTION EC-PROGRAM
    END-IF. DISPLAY G2.
    MOVE SPACES TO WS-VYBE-L
    STRING G2 DELIMITED SIZE INTO WS-VYBE-L
    IF WS-VYBE-L NOT = "0"
        DISPLAY "FAIL: want [0] got [" WS-VYBE-L "]"
        MOVE 1 TO RETURN-CODE
        RAISE EXCEPTION EC-PROGRAM
    END-IF. CALL 'CHILD'. DISPLAY G1.
    MOVE SPACES TO WS-VYBE-L
    STRING G1 DELIMITED SIZE INTO WS-VYBE-L
    IF WS-VYBE-L NOT = "2"
        DISPLAY "FAIL: want [2] got [" WS-VYBE-L "]"
        MOVE 1 TO RETURN-CODE
        RAISE EXCEPTION EC-PROGRAM
    END-IF. DISPLAY G2.
    MOVE SPACES TO WS-VYBE-L
    STRING G2 DELIMITED SIZE INTO WS-VYBE-L
    IF WS-VYBE-L NOT = "5"
        DISPLAY "FAIL: want [5] got [" WS-VYBE-L "]"
        MOVE 1 TO RETURN-CODE
        RAISE EXCEPTION EC-PROGRAM
    END-IF. STOP RUN. IDENTIFICATION DIVISION. PROGRAM-ID. CHILD. PROCEDURE DIVISION. ADD 1 TO G1. ADD 5 TO G2. EXIT PROGRAM. END PROGRAM CHILD. END PROGRAM T.

