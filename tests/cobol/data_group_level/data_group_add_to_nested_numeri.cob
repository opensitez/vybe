*> vybe-test: cobol/data_group_level/data_group_add_to_nested_numeric
*> origin: languages/cobol/tests/cobol/test_data_group_level.rs
IDENTIFICATION DIVISION.
PROGRAM-ID. T.
DATA DIVISION.
WORKING-STORAGE SECTION.
01 COUNTERS.
   05 GOOD-COUNT PIC 9(4) VALUE 100.
   05 BAD-COUNT PIC 9(4) VALUE 0.
01 WS-VYBE-L PIC X(256).
PROCEDURE DIVISION.
    ADD 1 TO GOOD-COUNT.
    ADD 1 TO BAD-COUNT.
    DISPLAY GOOD-COUNT.
    MOVE SPACES TO WS-VYBE-L
    STRING GOOD-COUNT DELIMITED SIZE INTO WS-VYBE-L
    IF WS-VYBE-L NOT = "0101"
        DISPLAY "FAIL: want [0101] got [" WS-VYBE-L "]"
        MOVE 1 TO RETURN-CODE
        RAISE EXCEPTION EC-PROGRAM
    END-IF.
    DISPLAY BAD-COUNT.
    MOVE SPACES TO WS-VYBE-L
    STRING BAD-COUNT DELIMITED SIZE INTO WS-VYBE-L
    IF WS-VYBE-L NOT = "0001"
        DISPLAY "FAIL: want [0001] got [" WS-VYBE-L "]"
        MOVE 1 TO RETURN-CODE
        RAISE EXCEPTION EC-PROGRAM
    END-IF.
    STOP RUN.

