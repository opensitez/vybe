*> vybe-test: cobol/class_clause/class_clause_runtime_zero_check
*> origin: languages/cobol/tests/cobol/test_class_clause.rs
IDENTIFICATION DIVISION.
PROGRAM-ID. CLS17.
ENVIRONMENT DIVISION.
CONFIGURATION SECTION.
SPECIAL-NAMES.
    CLASS ZERO-CLASS IS ZERO.
DATA DIVISION.
WORKING-STORAGE SECTION.
01 WS-VYBE-L PIC X(256).
01 CH PIC X.
PROCEDURE DIVISION.
    MOVE ZERO TO CH
    IF CH IS ZERO-CLASS
        DISPLAY "ZERO"
    END-IF
    STOP RUN.
    MOVE SPACES TO WS-VYBE-L
    STRING "ZERO" DELIMITED SIZE INTO WS-VYBE-L
    IF WS-VYBE-L NOT = "ZERO"
        DISPLAY "FAIL: want [ZERO] got [" WS-VYBE-L "]"
        MOVE 1 TO RETURN-CODE
        RAISE EXCEPTION EC-PROGRAM
    END-IF.

