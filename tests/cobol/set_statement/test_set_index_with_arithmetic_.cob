*> vybe-test: cobol/set_statement/test_set_index_with_arithmetic_step
*> origin: languages/cobol/tests/cobol/test_set_statement.rs
IDENTIFICATION DIVISION.
PROGRAM-ID. T.
DATA DIVISION.
WORKING-STORAGE SECTION.
01 WS-TABLE.
   05 WS-ITEM PIC 9(2) OCCURS 6 TIMES INDEXED BY WS-IDX.
01 WS-STEP PIC 99 VALUE 2.
01 WS-VYBE-L PIC X(256).
PROCEDURE DIVISION.

    MOVE 10 TO WS-ITEM(1).
    MOVE 20 TO WS-ITEM(3).
    MOVE 30 TO WS-ITEM(5).
    MOVE 1 TO WS-IDX.
    DISPLAY WS-ITEM(WS-IDX).
    MOVE SPACES TO WS-VYBE-L
    STRING WS-ITEM(WS-IDX) DELIMITED SIZE INTO WS-VYBE-L
    IF WS-VYBE-L NOT = "10"
        DISPLAY "FAIL: want [10] got [" WS-VYBE-L "]"
        MOVE 1 TO RETURN-CODE
        RAISE EXCEPTION EC-PROGRAM
    END-IF.
    SET WS-IDX UP BY WS-STEP.
    DISPLAY WS-ITEM(WS-IDX).
    MOVE SPACES TO WS-VYBE-L
    STRING WS-ITEM(WS-IDX) DELIMITED SIZE INTO WS-VYBE-L
    IF WS-VYBE-L NOT = "20"
        DISPLAY "FAIL: want [20] got [" WS-VYBE-L "]"
        MOVE 1 TO RETURN-CODE
        RAISE EXCEPTION EC-PROGRAM
    END-IF.
    SET WS-IDX UP BY WS-STEP.
    DISPLAY WS-ITEM(WS-IDX).
    MOVE SPACES TO WS-VYBE-L
    STRING WS-ITEM(WS-IDX) DELIMITED SIZE INTO WS-VYBE-L
    IF WS-VYBE-L NOT = "30"
        DISPLAY "FAIL: want [30] got [" WS-VYBE-L "]"
        MOVE 1 TO RETURN-CODE
        RAISE EXCEPTION EC-PROGRAM
    END-IF.
    STOP RUN.

