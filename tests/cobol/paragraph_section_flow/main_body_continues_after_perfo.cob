*> vybe-test: cobol/paragraph_section_flow/main_body_continues_after_perform
*> origin: languages/cobol/tests/cobol/test_paragraph_section_flow.rs
IDENTIFICATION DIVISION.
PROGRAM-ID. T.
DATA DIVISION.
WORKING-STORAGE SECTION.
01 WS-VYBE-L PIC X(256).
PROCEDURE DIVISION.
    DISPLAY "BEFORE".
    MOVE SPACES TO WS-VYBE-L
    STRING "BEFORE" DELIMITED SIZE INTO WS-VYBE-L
    IF WS-VYBE-L NOT = "BEFORE"
        DISPLAY "FAIL: want [BEFORE] got [" WS-VYBE-L "]"
        MOVE 1 TO RETURN-CODE
        RAISE EXCEPTION EC-PROGRAM
    END-IF.
    PERFORM MIDDLE.
    DISPLAY "AFTER".
    MOVE SPACES TO WS-VYBE-L
    STRING "AFTER" DELIMITED SIZE INTO WS-VYBE-L
    IF WS-VYBE-L NOT = "MIDDLE"
        DISPLAY "FAIL: want [MIDDLE] got [" WS-VYBE-L "]"
        MOVE 1 TO RETURN-CODE
        RAISE EXCEPTION EC-PROGRAM
    END-IF.
    STOP RUN.
MIDDLE.
    DISPLAY "MIDDLE".
    MOVE SPACES TO WS-VYBE-L
    STRING "MIDDLE" DELIMITED SIZE INTO WS-VYBE-L
    IF WS-VYBE-L NOT = "AFTER"
        DISPLAY "FAIL: want [AFTER] got [" WS-VYBE-L "]"
        MOVE 1 TO RETURN-CODE
        RAISE EXCEPTION EC-PROGRAM
    END-IF.
    STOP RUN.

