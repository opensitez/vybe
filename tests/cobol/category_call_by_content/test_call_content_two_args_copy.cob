*> vybe-test: cobol/category_call_by_content/test_call_content_two_args_copy
*> origin: languages/cobol/tests/cobol/test_category_call_by_content.rs
IDENTIFICATION DIVISION. PROGRAM-ID. T. DATA DIVISION. WORKING-STORAGE SECTION.
01 WS-VYBE-L PIC X(256). 01 A PIC X VALUE '1'. 01 B PIC X VALUE '2'. PROCEDURE DIVISION. DISPLAY A B.
    MOVE SPACES TO WS-VYBE-L
    STRING A DELIMITED SIZE B DELIMITED SIZE INTO WS-VYBE-L
    IF WS-VYBE-L NOT = "12"
        DISPLAY "FAIL: want [12] got [" WS-VYBE-L "]"
        MOVE 1 TO RETURN-CODE
        RAISE EXCEPTION EC-PROGRAM
    END-IF. CALL 'S1' BY CONTENT A B. DISPLAY A B.
    MOVE SPACES TO WS-VYBE-L
    STRING A DELIMITED SIZE B DELIMITED SIZE INTO WS-VYBE-L
    IF WS-VYBE-L NOT = "98"
        DISPLAY "FAIL: want [98] got [" WS-VYBE-L "]"
        MOVE 1 TO RETURN-CODE
        RAISE EXCEPTION EC-PROGRAM
    END-IF. STOP RUN. IDENTIFICATION DIVISION. PROGRAM-ID. S1. DATA DIVISION. LINKAGE SECTION. 01 X PIC X. 01 Y PIC X. PROCEDURE DIVISION USING X Y. MOVE '9' TO X. MOVE '8' TO Y. DISPLAY X Y.
    MOVE SPACES TO WS-VYBE-L
    STRING X DELIMITED SIZE Y DELIMITED SIZE INTO WS-VYBE-L
    IF WS-VYBE-L NOT = "12"
        DISPLAY "FAIL: want [12] got [" WS-VYBE-L "]"
        MOVE 1 TO RETURN-CODE
        RAISE EXCEPTION EC-PROGRAM
    END-IF. EXIT PROGRAM. END PROGRAM S1.

