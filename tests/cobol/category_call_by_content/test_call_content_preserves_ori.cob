*> vybe-test: cobol/category_call_by_content/test_call_content_preserves_original_for_alpha
*> origin: languages/cobol/tests/cobol/test_category_call_by_content.rs
IDENTIFICATION DIVISION. PROGRAM-ID. T. DATA DIVISION. WORKING-STORAGE SECTION.
01 WS-VYBE-L PIC X(256). 01 V PIC X VALUE 'A'. 01 W PIC X VALUE 'B'. PROCEDURE DIVISION. DISPLAY V W.
    MOVE SPACES TO WS-VYBE-L
    STRING V DELIMITED SIZE W DELIMITED SIZE INTO WS-VYBE-L
    IF WS-VYBE-L NOT = "AB"
        DISPLAY "FAIL: want [AB] got [" WS-VYBE-L "]"
        MOVE 1 TO RETURN-CODE
        RAISE EXCEPTION EC-PROGRAM
    END-IF. CALL 'S1' BY CONTENT V. DISPLAY V W.
    MOVE SPACES TO WS-VYBE-L
    STRING V DELIMITED SIZE W DELIMITED SIZE INTO WS-VYBE-L
    IF WS-VYBE-L NOT = "Z"
        DISPLAY "FAIL: want [Z] got [" WS-VYBE-L "]"
        MOVE 1 TO RETURN-CODE
        RAISE EXCEPTION EC-PROGRAM
    END-IF. STOP RUN. IDENTIFICATION DIVISION. PROGRAM-ID. S1. DATA DIVISION. LINKAGE SECTION. 01 P PIC X. PROCEDURE DIVISION USING P. MOVE 'Z' TO P. DISPLAY P.
    MOVE SPACES TO WS-VYBE-L
    STRING P DELIMITED SIZE INTO WS-VYBE-L
    IF WS-VYBE-L NOT = "AB"
        DISPLAY "FAIL: want [AB] got [" WS-VYBE-L "]"
        MOVE 1 TO RETURN-CODE
        RAISE EXCEPTION EC-PROGRAM
    END-IF. EXIT PROGRAM. END PROGRAM S1.

