*> vybe-test: cobol/move_group_redefines/redefines_char_extract_via_group
*> origin: languages/cobol/tests/cobol/test_move_group_redefines.rs
IDENTIFICATION DIVISION.
PROGRAM-ID. T.
DATA DIVISION.
WORKING-STORAGE SECTION.
01 RECORD PIC X(10) VALUE "HELLO-COBO".
01 WORDS REDEFINES RECORD.
   05 WORD1 PIC X(5).
   05 FILLER PIC X.
   05 WORD2 PIC X(4).
01 WS-VYBE-L PIC X(256).
PROCEDURE DIVISION.
    DISPLAY WORD1.
    MOVE SPACES TO WS-VYBE-L
    STRING WORD1 DELIMITED SIZE INTO WS-VYBE-L
    IF WS-VYBE-L NOT = "HELLO"
        DISPLAY "FAIL: want [HELLO] got [" WS-VYBE-L "]"
        MOVE 1 TO RETURN-CODE
        RAISE EXCEPTION EC-PROGRAM
    END-IF.
    DISPLAY WORD2.
    MOVE SPACES TO WS-VYBE-L
    STRING WORD2 DELIMITED SIZE INTO WS-VYBE-L
    IF WS-VYBE-L NOT = "COBO"
        DISPLAY "FAIL: want [COBO] got [" WS-VYBE-L "]"
        MOVE 1 TO RETURN-CODE
        RAISE EXCEPTION EC-PROGRAM
    END-IF.
    STOP RUN.

