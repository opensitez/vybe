*> vybe-test: cobol/occurs_indexed_by/occurs_index_set_then_move_multiple
*> origin: languages/cobol/tests/cobol/test_occurs_indexed_by.rs
IDENTIFICATION DIVISION.
PROGRAM-ID. T.
DATA DIVISION.
WORKING-STORAGE SECTION.
01 T.
   05 VAL PIC 9(2) OCCURS 4 TIMES INDEXED BY IDX.
01 WS-VYBE-L PIC X(256).
PROCEDURE DIVISION.
    SET IDX TO 1.
    MOVE 11 TO VAL(IDX).
    SET IDX UP BY 1.
    MOVE 22 TO VAL(IDX).
    SET IDX UP BY 1.
    MOVE 33 TO VAL(IDX).
    DISPLAY VAL(1).
    MOVE SPACES TO WS-VYBE-L
    STRING VAL(1) DELIMITED SIZE INTO WS-VYBE-L
    IF WS-VYBE-L NOT = "11"
        DISPLAY "FAIL: want [11] got [" WS-VYBE-L "]"
        MOVE 1 TO RETURN-CODE
        RAISE EXCEPTION EC-PROGRAM
    END-IF.
    DISPLAY VAL(2).
    MOVE SPACES TO WS-VYBE-L
    STRING VAL(2) DELIMITED SIZE INTO WS-VYBE-L
    IF WS-VYBE-L NOT = "22"
        DISPLAY "FAIL: want [22] got [" WS-VYBE-L "]"
        MOVE 1 TO RETURN-CODE
        RAISE EXCEPTION EC-PROGRAM
    END-IF.
    DISPLAY VAL(3).
    MOVE SPACES TO WS-VYBE-L
    STRING VAL(3) DELIMITED SIZE INTO WS-VYBE-L
    IF WS-VYBE-L NOT = "33"
        DISPLAY "FAIL: want [33] got [" WS-VYBE-L "]"
        MOVE 1 TO RETURN-CODE
        RAISE EXCEPTION EC-PROGRAM
    END-IF.
    STOP RUN.

