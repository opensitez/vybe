// vybe-test: js/async_generator_function_prototype/async_generator_nested_declaration_keeps_outer_name
// origin: languages/js/tests/js/test_async_generator_function_prototype.rs

function __line(...args) {
    // console.log joins its arguments with a single space. String() is the
    // coercion Vybe's logging host applies to each one.
    return args.map(String).join(" ");
}

function __check(got, want) {
    if (got !== want) {
        console.log("FAIL: want [" + want + "] got [" + got + "]");
        throw new Error("assertion failed");
    }
}

function outer() { async function* inner() { yield 1; } return inner.name; } __check(__line(outer()), "inner");
