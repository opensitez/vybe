// vybe-test: js/generator_function_prototype/generator_function_from_new_function_is_not_generator_function
// origin: languages/js/tests/js/test_generator_function_prototype.rs

function __line(...args) {
    // console.log joins its arguments with a single space. String() is the
    // coercion Vybe's logging host applies to each one.
    return args.map(String).join(" ");
}

function __check(got, want) {
    if (got !== want) {
        console.log("FAIL: want [" + want + "] got [" + got + "]");
        throw new Error("assertion failed");
    }
}

const f = new Function("return 1;"); __check(__line(f instanceof GeneratorFunction), "false");
